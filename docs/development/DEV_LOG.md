# BantAI Development Log

## Project Modules

| Module | Description |
|--------|-------------|
| **Backend** | NestJS API server (Prisma + PostgreSQL) for SMS analysis, model inference, and data storage |
| **Dashboard** | React + Vite web dashboard for monitoring detections and system management |
| **Android** | Kotlin Android app with real-time on-device smishing detection |
| **ML** | Machine learning model training, evaluation, and export pipeline |
| **Datasets** | SMS datasets (raw, processed, labeled) used for model training |

---

## Progress Log

| Date | Module | What was done | Done by |
|------|--------|---------------|---------|
| 2026-07-03 | All | Initial commit: monorepo structure (`mobile/`, `web/`, `backend/`, `ai/`, `docs/`) + Android app phases 1–7 (onboarding flow, main screens, SMS receivers, local keyword-based detection) | Gio |
| 2026-07-09 | Backend | Backend framework setup: NestJS project scaffold, module skeletons (`sms`, `users`, `campaigns`, `reports`, `analytics`, `ai`, `health`), Prisma + PostgreSQL wiring, initial schema/migration (`User`, `SmsMessage`, `Classification`, `Alert`) | Reymark De Castro |
| 2026-07-15 | Backend | User authentication module (v1): password-based auth with Prisma and bcrypt, `auth_user` migration (PR #2, #4) | Reymark De Castro |
| 2026-07-16 | Backend | Auth v2: replaced password login with phone OTP flow (`register`, `request-otp`, `verify-otp`), JWT issuance on verification, `otp_auth` migration adding `OtpCode` model and dropping `passwordHash` (PR #5) | Reymark De Castro |
| 2026-07-17 | Dashboard | Web dashboard UI (hardcoded/mock data): landing, login, 2FA, request-access pages; client dashboard (overview, messages, campaigns, analytics, export, help, settings); admin dashboard (overview, reports, model, concept drift, dataset, classification, FP/FN, campaigns, timeline, users, export, server/API/DB logs, tips, settings) (PR #7) | Daryl De Castro |
| 2026-07-17 | Backend | Auth integration fixes: installed missing JWT/passport dependencies in `backend/` workspace, regenerated stale Prisma client, applied pending `otp_auth` migration, added `PassportModule` import, added protected `GET /auth/me` endpoint, documented `JWT_SECRET`/`JWT_EXPIRES_IN` in `.env.example`. Full OTP → JWT → protected-route flow smoke-tested end to end against a live server | Gio |
| 2026-07-17 | Docs | Documentation baseline: filled in progress log, added agile process document (`AGILE_PROCESS.md`) and auth API reference (`docs/api/auth.md`) | Gio |
| 2026-07-17 | Android | Connected mobile app to backend auth: new `AuthApi` HTTP client (request-otp, verify-otp), OTP screens registered into onboarding nav flow (Default SMS → Confirm Number → Enter Code → Profile), JWT + phone saved to DataStore, loading/error states on auth screens, INTERNET permission + dev-only cleartext config for localhost/10.0.2.2 | Gio |
| 2026-07-17 | Backend | Profile sync: implemented `users` module with JWT-guarded `PUT /users/me` (updates firstName/lastName/email), registered `UsersModule` in app module | Gio |
| 2026-07-17 | Android | Profile screen syncs name to backend on Continue using stored JWT, so the `User` row is fully populated (phone + names). Verified end to end on a physical device (Huawei VOG-L29 via USB + `adb reverse`): OTP login recorded the phone number and profile names in Postgres | Gio |
| 2026-07-18 | Android | Onboarding restyle to match interior iOS-minimal look: new shared components (`OnboardingComponents.kt` — PrimaryButton, PillTextField, OnboardingHeader, GroupedCard, AccentIconTile, FeatureListRow); all five active onboarding screens converted to full-bleed black, 32sp large titles, muted uppercase eyebrows, borderless pill inputs, grouped SurfaceElevated cards with hairline dividers, sentence-case buttons. Default-SMS screen rebuilt from floating card to full-bleed page. Verified full flow on device | Gio |
| 2026-07-18 | Android | Sender avatars replaced with neutral iOS-style silhouette (`SenderAvatar.kt`): gray person placeholder everywhere (system senders like GLOBE always get it); shows the contact's photo only when one is set (new `READ_CONTACTS` permission + cached ContactsContract lookup). Applied in Messages list, Home recent messages, and conversation bubbles | Gio |
| 2026-07-18 | Android | Conversation detail upgrades: unknown-classification threads show "Suspicious messages detected — tap to report" banner routing to the report flow; AI Summary button now always visible on every conversation (legit included), opening `AISummaryBottomSheet` with a verdict variant ("Suspicious 68%" vs "Looks safe 92%" — content still mock, pending backend AI wiring) | Gio |
| 2026-07-18 | Docs | Doc sync for earlier pushes that shipped without doc updates: rewrote `backend/README.md` (password-auth era → current OTP/JWT auth, users endpoint, correct Docker port 5433 + `--schema` migration commands, JWT env vars), linked users API reference from root README | Gio |
| 2026-07-18 | Android | Collapsing tab bar (`MainScreen.kt`): the floating glass bar now rests as a single extra-translucent pill showing only the active tab (with alert dot when off the Alerts tab); tapping the pill expands the full four-tab bar, selecting a tab navigates and collapses it again, and tapping outside the expanded bar dismisses it. Animated with `animateContentSize`; state survives rotation. Verified on device | Gio |
| 2026-07-20 | Docs | SMS ingestion API contract (`docs/api/sms.md`): defined `POST /api/sms/ingest` request/response shape, routing rules (≥0.90 blocked · 0.50–0.89 alert · <0.50 inbox), and error responses | Reymark De Castro |
| 2026-07-20 | Backend | `MessageFeature` Prisma model added to schema: stores `normalizedBody` and `maskedBody` per SMS for ML auditability; migration `add_message_features` | Reymark De Castro |
| 2026-07-20 | Backend | SMS ingestion endpoint implemented (`POST /api/sms/ingest`, JWT-guarded): stores raw SMS, runs placeholder preprocessing + keyword classifier, writes `MessageFeature` + `Classification` rows, returns label/score/action; `SmsModule` registered in `AppModule` | Reymark De Castro |
| 2026-07-20 | Backend | Fixed Prisma ↔ Docker DB connectivity: diagnosed three-layer bug (UTF-8 BOM in pg_hba.conf blocking all reloads, auth method mismatch scram-sha-256 vs md5, and native Windows postgres.exe on port 5433 intercepting connections before Docker); changed Docker port mapping to `5434:5432`, updated `.env` and `.env.example`; `PrismaService` now passes `DATABASE_URL` explicitly via constructor override. Full auth→ingest flow smoke-tested end-to-end | Reymark De Castro |
| 2026-07-21 | Backend | Backend audit, bug fixes, and unit test suite (49 tests, all passing). Bugs fixed: (1) old OTPs not invalidated on re-request — added `updateMany` to mark previous codes verified before issuing a new one, preventing OTP replay; (2) `[URL]` keyword in placeholder classifier was uppercase while comparison ran on lowercase input, making "Likely Smishing" label unreachable — corrected to `[url]`; (3) `@prisma/client` moved from devDependencies to dependencies (was missing from production installs); (4) added `@MaxLength(64)` to `IngestSmsDto.sender`. Unit tests written for `AuthService` (register, requestOtp, verifyOtp, getMe), `SmsService` (ingest, preprocess, classify, route — including all threshold boundary cases), `UsersService` (updateMe), and all three controllers (AuthController, SmsController, UsersController) | Reymark De Castro |
| 2026-07-21 | ML | Track B AI/ML pipeline foundation (Sprint 1). Scaffolded Python FastAPI inference service (`ai/service` — `GET /health`, `POST /classify`, lazy XLM-RoBERTa loading, returns 503 until a model exists). Drafted privacy masking + NFKC preprocessing (`ai/preprocessing` — regex masking of URL/PHONE/OTP/AMOUNT tuned for PH phone/currency formats + Unicode NFKC, shared identically by train and inference). Set up XLM-RoBERTa fine-tuning environment (`ai/training` — 3-label head "Likely Smishing/Suspicious/Unknown", AdamW hyperparams, SentencePiece tokenizer w/ 250K vocab check, dataset loader with 80/20 stratified split, HuggingFace Trainer script). Added pytest suite (masking/normalization/pipeline/service/config), `requirements.txt`, `.env.example`, `pyproject.toml`, `ai/README.md`, and a `sample.csv` format reference. Model not yet fine-tuned (awaits the PH smishing dataset + GPU — Sprint 2) | Maxene |
| 2026-07-22 | ML | Sprint 2 Track B — 3 of 4 tasks complete (1 blocked by dataset). (1) Completed privacy masking: added EMAIL/URL/PHONE/AMOUNT/OTP placeholders + edge cases (₱5k shorthand, hxxp de-fanged links, phone numbers without separators); 27 tests pass. (2) Relabeled AI from "Likely Smishing/Suspicious/Unknown" to "Ham/Spam/Scam" across training config, dataset loader, sample data. (3) Implemented softmax confidence head + threshold routing to 4 user buckets (Safe/Unknown/Spam/Blocked); routing tests pass (12/12). (4) Wrote labeling guide (`LABELING_GUIDE.md`) + Google Colab training path. All 3 buildable tasks verified. Blocked: fine-tuning (Task 4) awaits labeled Ham/Spam/Scam dataset + GPU. `/classify` endpoint now returns full softmax distribution + derived bucket for backend integration. Thresholds tunable (safe≥0.50, spam≥0.60, blocked≥0.90). | Maxene |
| 2026-07-22 | ML | Label scheme refinement (pre-labeling). Kept the 3 dataset labels (Ham/Spam/Scam) but **redefined** them to be intent-based and gap-free — Ham now covers any honest/harmless message incl. unsolicited-but-non-deceptive alerts and wrong numbers, removing the old "unknown-sender promo" workaround rule. Renamed the user-facing buckets for clarity (Safe/**Review**/Spam/Blocked; "Unknown"→"Review", "unknown" bucket id retained internally). Added `LABEL_DEFINITIONS.md` documenting both layers + the mapping. Hardened routing: `route()` now consumes the full softmax distribution and applies **argmax → gap check → threshold**; new `review_margin` (0.15) sends near-ties (e.g. 0.50/0.50/0.00) to Review instead of letting argmax name a false winner. Rewrote `test_routing.py` for the distribution signature + tie cases (15 assertions; not run locally — Python not on this machine). | Maxene |
| 2026-07-23 | Dashboard | Web dashboard visual pass (commit `50d72ca`, titled "slight visual edits" but substantially larger in scope — 26 files, ~3,000 lines changed). New components (`SectionHeader`, `StatCounter`, `StepCard`, `ThreatCard`), new public pages (`About`, `HowItWorks`, `Research`), new `ProtectedRoute` wrapper, scroll-reveal animation hooks (`useReveal`, `useScrollReveal`), a basic `lib/auth.ts` helper, expanded `AppRoutes.tsx`, and a large rewrite of `styles.css`. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Gio |
| 2026-07-24 | Backend | Backend ↔ AI service integration (PR `feature/backend-ai-integration`). Replaced hardcoded `AiService` stub with a real `fetch` call to `POST /classify` on the FastAPI ML service; label translation layer maps AI classes (Ham/Spam/Scam) to backend labels (Unknown/Suspicious/Likely Smishing) and bucket translation maps AI routing decisions (safe/unknown/spam/blocked) to actions (inbox/alert/blocked). Full AI response now persisted — `Classification` table extended with `scores` (jsonb, full softmax distribution) and `bucket` (text, AI routing decision) via new Prisma migration `20260724000000_add_ai_fields_to_classification`. AI's `masked_text` replaces local-masked text in `MessageFeature`. Graceful fallback to local keyword heuristic when AI service is unreachable or returns 503. Local fallback masking updated to include email masking (gap vs AI service). Created `AiModule`; wired into `SmsModule` and `AppModule`. `AI_SERVICE_URL` env var added. | Reymark De Castro |
| 2026-07-27 | Backend | Clustering, sender verification, and link suppression foundations (commit `298aa1e`). Prisma schema grew five models: `Contact` (synced phone-book entries per user), `CampaignCluster` (`centroid` Json for cosine similarity, `urlDomains`, `isActive`, `messageCount` — populated by the AI/ML service), `ExplainableIndicator` (one-to-one with `Classification`, stores the SHAP tag list), `BlockedNumber`, and a link-suppression record keyed by `messageId`. `CampaignsController`/`CampaignsService` went from an empty scaffold to real endpoints (`findAll` active clusters, `findOne` with up to 50 recent messages, `create`/`addDomains` for the AI service to register and grow clusters). `sms.service.ts` and `ai.service.ts` extended to call into this — the point where campaign matching first became possible end to end. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Reymark De Castro |
| 2026-07-29 | Dashboard | Major web dashboard build-out (commit `68723c9`, 53 files, ~16,400 lines added). New chart components (`BarChart`, `DonutChart`, `LineAreaChart`), common UI components (`Badge`, `Button`, `Input`, `Modal`, `ShieldLogo`), a design-token/component stylesheet system (`tokens.css`, `components.css`, `animations.css`), and new pages (`About`, `AdminLogin`, `HowItWorks`, `Landing`, `Login`, `Profile`, `Research`, `Settings`) — `admin.tsx`, `client.tsx`, and `public.tsx` all grew substantially. Also added `UserAvatarContext`, `useClickOutside`/`useTimer` hooks, shared TypeScript types (`auth.ts`, `common.ts`, `licensing.ts`, `threat.ts`), and form validation utilities. Commit message notes it couldn't be pushed to `develop` directly at the time. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Daryl De Castro |
| 2026-07-29 | Dashboard | Fixed build/runtime errors from the previous commit (`d33e1b7`) — rewrote `Footer.tsx` and the `About`/`HowItWorks`/`Research` pages, adjusted `public.tsx` and `AppRoutes.tsx`. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Daryl De Castro |
| 2026-07-29 | Backend | Added `StoreIndicatorsDto` (`backend/src/sms/dto/store-indicators.dto.ts`) — request-body validation for the SHAP explainability endpoint the AI service posts indicator tags to. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Reymark De Castro |
| 2026-07-29 | ML | Dataset build/review pipeline (`build_dataset.py`, `apply_review_corrections.py`) plus first XLM-RoBERTa fine-tune (Sprint 2 Track B, task 2.3.4). Consolidated dataset to 15,728 rows (Ham 9,309 / Spam 3,799 / Scam 2,620) from Kaggle + NTC + real phone-inbox exports; 596 rows human-reviewed across 4 review rounds. Several rule bugs found and fixed along the way — train/val leakage via privacy masking, a dummy-data leak, promo-lexicon gaps, brand-impersonation false positives on legitimate marketing, narrow PSA/advisory phrasing, and no whitelist concept for institutional (`.gov.ph`/`.edu.ph`) domains (full detail in `ai/PIPELINE.md` Bug History §1–§11). Fine-tuned XLM-RoBERTa-base with class-weighted loss on an 80/20 stratified split (seed 42, 0.00% leakage confirmed pre-training): 95.44% accuracy / 94.38% macro-F1. Added bucket-level (post-threshold-routing) evaluation (`scripts/evaluate_buckets.py`), using the real production `route()` function: 3.58% worst-case scam-miss rate, 1.15% false-alarm rate, 1.0% unknown rate. Full writeup: `docs/development/AI_MODEL_RESULTS.md` | Maxene |
| 2026-07-29 | Dashboard | UI adjustment pass (commit `278d3ca`): `ShieldLogo`, `About`, `HowItWorks`, `Landing`, and `Research` pages, plus `components.css` tweaks. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Daryl De Castro |
| 2026-07-30 | ML | Two new raw-inbox exports (1,050 + 447 genuinely-new messages) folded into the dataset. Audited and closed two `.gitignore` gaps found while doing so — the Colab upload package embeds a full copy of the labeled dataset (real personal SMS) and was untracked-but-not-ignored, same for the 1GB+ trained-model directory. Ran two more full-population review rounds (5, 6) on the new batches, then a first-ever backlog review (round 7: 1,004 raw-inbox low-confidence rows that rounds 1–6 had never reached, since they only ever covered newly-arrived data) — 34% disagreement rate, by far the highest of any round. Found and fixed 6 rule bugs (`ai/PIPELINE.md` Bug History §12–§14): an apostrophe breaking an anti-scam-advisory phrase match, a non-Latin-domain regex false-firing on peso-amount decimals (mislabeling real GRAB/TikTok transactional messages as Scam), Lazada's official short-link domain missing from the whitelist, word-boundary plural misses ("lucky winner" vs "winners", "cash loan" vs "loans"), no rule coverage for Messenger-link job scams or credit-card ID-harvesting-via-email scams, and a telco/e-wallet loyalty-promo vocabulary gap (289 legitimate GlobeOne/DITO/GCash messages were defaulting to Ham). Dataset now 16,772 rows (Ham 9,315 / Spam 4,788 / Scam 2,669); 1,613 rows (9.6%) human-reviewed across 7 rounds; 0 duplicate rows; 0.00% train/val leakage reconfirmed; all 46 tests passing. Colab package and docs resynced to the corrected dataset. Not yet retrained — the currently-installed model still reflects the 15,728-row snapshot from 2026-07-29; next retrain deliberately deferred until more raw data accumulates, to avoid retraining on every incremental correction | Maxene |
| 2026-07-30 | ML | Sprint 3 Track B — wired campaign memory + verified SHAP against the real model. Campaign centroids now load at service startup (`service/centroid_source.py` + FastAPI lifespan hook): previously `CampaignMatcher` booted empty and reported "no campaign" for every message forever — the matching logic was correct but nothing ever fed it. Default source is the local `campaign_clusters.json` (works with no backend and no database); the backend-fetch path is written and tested but switched off, **blocked on a one-line Track A change** — `campaigns.service.ts:findAll()` uses an explicit `select` that omits `centroid`, which is the only field the matcher needs (noted in-code for Reymark; suggest a separate internal route rather than fattening the list every client fetches, since a centroid is 768 floats). Verified 221 centroids loading correctly at startup. Ran real SHAP against the fine-tuned model for the first time — it works, and timings settle the design question: classification ~50 ms vs SHAP 13–26 s, roughly 300–500× slower, so asynchronous explanation is required rather than merely tidy. That verification surfaced three real bugs, all now fixed with regression tests (`ai/PIPELINE.md` §15–§17): (1) campaign matching ran against Ham messages, and a real personal message matched a money-transfer cluster at 0.96 — clusters are built from Spam+Scam only, so matching Ham against them is meaningless; now gated on label. Measured Ham-vs-Scam similarity averages −0.008 with only 0.8% above threshold, confirming the embedding space is sound and the bug was the population, not the 0.85 threshold. (2) The SHAP token→tag mapper used substring matching, so the Tagalog prefix "mag" matched the keyword "mag-ingat" and confidently mislabeled a gambling blast as "Urgency Cue" — a wrong tag being worse than none; replaced with whole-word matching plus a prefix allowance for SentencePiece's mid-word splits. (3) The Gambling Bait vocabulary was English-only, so a real Tagalog blast ("Magparehistro… tumaya…") produced no tag at all — the third time this exact class of bug has appeared after PROMO_TL and JOB_SCAM, so every new lexicon in this project should now be checked for Tagalog/Taglish coverage before shipping. Also replaced the deprecated FastAPI `on_event` startup hook with the modern lifespan pattern. 138 tests passing (up from 123). Not committed. | Maxene |
| 2026-07-30 | ML | Sprint 3 Track B build-out — all remaining Track B items (3.2.1, 3.2.2, 3.3.4–3.3.7, 3.4.1, 3.4.2). Campaign clustering implemented as the manuscript's two-speed design: fast path (`service/campaign.py`, cosine ≥0.85 vs. active centroids, per message) and slow path (`scripts/cluster_campaigns.py`, offline HDBSCAN with `min_cluster_size=5`). Both branches share one 768-dim `[CLS]` embedding per manuscript Stage 4/5 — `classify_full()` now does a single forward pass with `output_hidden_states=True` and returns the class distribution and the embedding together, rather than computing them twice and risking two different semantic spaces. Clustering uses `sklearn.cluster.HDBSCAN` (no extra dependency; sklearn was already required) with euclidean distance on L2-normalized vectors, which is monotonically equivalent to cosine so both paths agree on "similar". SHAP explainability (`service/explainer.py`) computes real Shapley values and maps top tokens through the 3.1.2 tag dictionary; it runs **asynchronously** and posts to the backend's existing `POST /sms/:messageId/indicators` endpoint, because true SHAP on a transformer takes seconds per message and would otherwise break real-time interception — `shap` is optional and the module degrades to the keyword tagger, recording which path ran. Scam awareness cards (`service/tips.py`, 9 cards) resolve cluster → admin override → dominant indicator tag → generic fallback, so newly discovered campaigns always get a card instead of a blank. Specs written for the other tracks to build against: `docs/api/campaigns.md` (3.2.1) and `docs/api/explainability.md` (3.2.2); `docs/api/classify.md` updated for the additive `campaign` response field. **First real clustering run:** embedded all 16,772 messages (20.3 min CPU) and clustered the 7,457 Spam+Scam population → 221 clusters, 40.1% grouped, largest cluster 2.8% of population, median size 8. The manuscript's 0.85 / min_cluster_size=5 are now validated on real Philippine SMS rather than merely proposed — neither failure mode (giant blob / fragmentation) occurred. Strongest result: only **5 of 221 clusters mix Spam and Scam** despite clustering never seeing the labels, independently corroborating that the embedding space separates honest marketing from fraud. Two bugs found and fixed during the run: HDBSCAN needs density contrast (a buffer holding one campaign and nothing else returns all-noise — real constraint, now documented and locked in a test; my test had asserted the wrong semantics), and the results JSON was being written *after* the console print loop, so a `UnicodeEncodeError` on a peso sign destroyed 20 minutes of work — persistence now happens before display. Also closed a privacy gap: `embeddings.npz` bundles real message text but `.gitignore` only covered `.csv`/`.json` in `datasets/processed/`. 123 tests passing (up from 60). Not committed. | Maxene |
| 2026-07-30 | ML | Sprint 3 Track B kickoff (WBS 3.1.2). Read the actual thesis manuscript (`THESONE_Group-7_Full-Manuscript.pdf`) for the first time this session to ground Sprint 3 work rather than guessing — confirmed the classification/clustering/explainability pipeline spec exactly: cosine similarity ≥0.85 against active campaign-cluster centroids, else buffered for offline HDBSCAN re-clustering with `min_cluster_size=5` (matches WBS verbatim); critically, the classification and clustering branches **share the same 768-dim [CLS] embedding** from the XLM-RoBERTa encoder rather than using separate pipelines — a real architectural constraint for how 3.2.1/3.3.4 get built. Also confirmed the manuscript's three named example SHAP indicator tags ("Prize Lure," "Suspicious URL," "Brand Impersonation," "Urgency Cue" — described as illustrative, not an exhaustive list). Drafted and unit-tested the indicator tag dictionary (`service/indicator_tags.py`, 9 tags: the 4 manuscript-named ones plus 5 more grounded in the dataset's own validated rule vocabulary — Gambling Bait, Fake Job Offer, Unsolicited Credit Offer, Personal Info Request, OTP/Account Phishing); 14 new tests, all passing, output shape matches the backend's already-existing `POST /sms/:messageId/indicators` contract (`StoreIndicatorsDto`, found already built from a prior commit). Separately fixed `docs/api/sms.md`, which had drifted from actual backend behavior — it documented `classification.label` as the manuscript's old "Likely Smishing/Suspicious/Unknown" wording, but `sms.service.ts` actually returns the AI service's `Ham`/`Spam`/`Scam` unmodified, and the response also includes `senderStatus`/`suppressedLinks` fields the doc never mentioned; also documented the previously-undocumented indicators endpoint. Manuscript class-name wording vs. shipped code remains a known, separately-tracked mismatch (adviser-facing) — this fix makes the API doc match the code, not the manuscript prose. | Maxene |
| 2026-07-31 | Backend | Added `GET /campaigns/centroids` (commit `5dd8e30`) — internal endpoint returning each active `CampaignCluster`'s embedding vector, so the AI service can load real campaign centroids from the backend instead of only the local `campaign_clusters.json` file. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time; see the 2026-08-04 ML entry for why the AI service didn't actually start using it until then.)* | Reymark De Castro |
| 2026-07-31 | Android | Closed the Sprint 2 integration gap (WBS 2.4.3): the phone now classifies through the backend instead of on-device keywords. Until now `SmsReceiver` called `SmsRepository.classifyMessagePublic()` — a ~20-entry keyword list — and nothing on the device ever called `POST /api/sms/ingest`, so the fine-tuned XLM-RoBERTa model never reached a real user. New `SmsApi` client (plain `HttpURLConnection` + `org.json`, mirroring the existing `AuthApi` rather than adding Retrofit/OkHttp) posts sender/body/ISO-8601 `receivedAt` with the stored JWT and maps the backend's `action` onto the existing notification branches (`blocked` → system block + smishing alert, `alert` → suspicious alert, `inbox` → silent); an unrecognised action deliberately routes to the inbox so a protocol change can never silently hide a message. Network work runs under `goAsync()` with a 5s timeout, sized against the ~10s broadcast-receiver budget so the fallback still has room to run; the ContentProvider insert was moved ahead of the network call so a message still lands in the inbox when the backend is unreachable. The keyword heuristic is retained verbatim as the offline fallback (no token, timeout, non-2xx, or no connectivity), so behaviour is unchanged when the device is offline. Also extracted the hardcoded `http://localhost:3000/api` out of `AuthApi` into `ApiConfig`, now fed by a `BACKEND_BASE_URL` buildConfigField, so switching between emulator (`10.0.2.2`) and USB device no longer needs a source edit. `compileDebugKotlin` passes. | Gio |
| 2026-07-31 | Backend | Sprint 2 integration test suite (WBS 2.4.3) — `test/sms-ingest.e2e-spec.ts`, 6 cases covering the exact payload the Android client sends: AI-service happy path (message + `MessageFeature` + `Classification` all persisted, `suppressedLinks` populated for a bit.ly scam), AI-down fallback (503 → `classifyLocally`, null bucket, local masking still applied), auto-block then suppression of the sender's next message, JWT guard, benign-message inbox routing, and `receivedAt` validation. `AiService` is overridden so the suite is hermetic and needs no Python service in CI; OTP is read straight from the `OtpCode` table to complete the real onboarding flow. Writing it surfaced three pre-existing defects, all fixed: (1) **auto-block gated on raw `score >= 0.9`**, but `score` is the winning class's confidence, not the scam probability — a confidently-*Ham* message (Ham at 0.94) would auto-block a legitimate sender; now gated on the routing decision (`action === 'blocked'`), which leaves the local-fallback path's behaviour unchanged since `routeFromScore` already blocks at ≥0.90. (2) `src/sms/dto/store-indicators.dto.ts` had **unresolved merge-conflict markers committed to `develop`** (both sides functionally identical, differing only in class name and `!`), so the backend did not compile at all. (3) The Sprint 3 models (`Contact`, `BlockedNumber`, `CampaignCluster`, `ExplainableIndicator`, `SenderVerificationCache`) existed in `schema.prisma` but **had no migration** — `sms.service.ts` ingest reads `blockedNumber` and `contact`, so ingest failed against a freshly-migrated database; generated and applied `20260731081048_add_sprint3_clustering_and_verification_tables` (closes WBS 3.3.1). Also replaced the leftover `nest new` scaffold e2e test (asserted `GET /` → "Hello World!", a route this project never had, failing since the scaffold was replaced) with a real `/api/health` smoke test. Local `.env` corrected from port 5433 to 5434 to match `docker-compose.yml`. Backend suite: 77 unit + 8 e2e passing. | Gio |
| 2026-08-04 | Docs | WBS sync: `develop` had drifted from the actual merged state — PR #18 (`5fffc0b`, campaign clustering + SHAP explainability) merged 2026-07-31 but the WBS checkboxes were never flipped. Flipped 3.1.2, 3.2.1, 3.2.2, 3.3.4, 3.3.5, 3.3.6, 3.3.7, 3.4.1, 3.4.2 to complete; Sprint 3 Track B is now 8/9 items done, only 3.1.2's dictionary-review pass remained open going into this session (closed same day, see below). Parent items 3.1–3.4 stay in-progress — each still has an open Track A/C/All-members child. | Maxene |
| 2026-08-04 | ML | Wired the AI service to the real backend centroids endpoint (`ai/service/centroid_source.py`). Reymark shipped `GET /campaigns/centroids` on 2026-07-31 (commit `5dd8e30`), but `load_from_backend()` was still requesting the old `/campaigns` route, which never returns `centroid` — so even with the blocker resolved on the backend side, the AI service was silently still reading the local `campaign_clusters.json` file. Fixed the URL, flipped `centroid_source`'s default from `"file"` to `"backend"` (`file`/`none` remain available as fallback/disable), and documented `BANTAI_AI_CENTROID_SOURCE`/`BANTAI_AI_BACKEND_URL` in `ai/.env.example` (previously undocumented there). Documented the new endpoint in `docs/api/campaigns.md`. All 10 `test_centroid_source.py` tests still pass — they mock `urlopen` without asserting the URL, so the path change was safe. **Verified live end to end:** seeded all 221 clusters from the 2026-07-30 run into the dev database, confirmed `GET /campaigns/centroids` serves 221×768-dim vectors, and confirmed the AI service loads all 221 *from the backend* (returned ids are database UUIDs, not the local file's integer cluster ids — proof it is not silently falling back to the file). A real scam SMS then classified Scam @ 0.9988 → `blocked` and matched campaign `f70d27e6…` at 0.9933 similarity, i.e. the full masking → classification → campaign-matching path works against backend-sourced centroids. **Gap found while doing this:** nothing in the codebase pushes `cluster_campaigns.py` output into the backend — the 221 clusters had to be POSTed ad hoc, so a proper sync script (or a `POST /campaigns/bulk`) is still missing; without it a fresh database leaves campaign matching silently inactive. Also noted: the startup "Loaded N campaign centroids" line is `logger.info` and uvicorn does not configure the root logger, so success is invisible in the service log (the failure path is `logger.warning`, which does surface via Python's last-resort handler). | Maxene |
| 2026-08-04 | ML | Sprint 4 Track B head start — the items that carry no Track A dependency (4.1.2, 4.2.2, 4.2.3, 4.3.6, 4.3.7, 4.3.9, 4.4.2). New `ai/retraining/` package, deliberately pure (no DB, no model loading, no HTTP) so the policy stays unit-testable and the NestJS hourly cron (4.3.3) can own *scheduling* without duplicating *policy*. (1) **Triggers** (`triggers.py`) — resolved the three thresholds the manuscript states loosely: "50 samples" = 50 admin-**validated** reports (counting raw submissions would let one user force retraining at will), "F1 drop 5%" = 5 percentage **points absolute** (at the 0.9438 baseline the absolute/relative readings differ materially, and absolute is stricter), and macro-F1 rather than accuracy since at ~62/21/17 class balance accuracy can hold while Scam recall collapses. Page-Hinkley catches the slow slide a fixed floor never trips until it is already severe. (2) **Reservoir sampling** (`sampling.py`) — Vitter's Algorithm R, one pass, fixed memory, no advance knowledge of population size, so snapshots stream out of the database instead of materialising 16k+ rows to shuffle; taking "the newest N rows" instead would over-represent whichever campaign was active that week. (3) **Promotion gate** (`promotion.py`) — F1 floor checked *before* McNemar because an absolute safety property must not be overridable by a significance result; exact binomial rather than chi-square since the discordant count is often only tens of rows. (4) **TF-IDF summarization** (`service/summarize.py`, 4.3.9) — extractive by design: an abstractive summariser can hallucinate an amount or deadline into a message the sender never wrote, which in a product where the user is deciding whether to trust a message is worse than no summary. Three real bugs found by the tests, all fixed: **Page-Hinkley had the slack sign inverted** (the decrease-detecting form adds where the textbook increase form subtracts — as written, perfectly stable input accumulated −δ per step and false-alarmed on constant data); **`random.Random()` rejects a tuple seed**, so per-stratum sampling raised `TypeError` (restructured to one string-seeded RNG per stratum, which also means adding a label later no longer perturbs existing strata); and **TF-IDF mean scoring ranked brevity, not informativeness** — sklearn L2-normalizes rows by default, so a short sentence concentrates its weight and "Good morning po." outranked the sentence carrying the actual refund amount (fixed with `norm=None`). Self-review before committing caught three more things, all addressed: **`scipy` was imported directly but only present transitively** via scikit-learn (now declared in `requirements.txt` — a transitive pin is free to disappear); **`service/summarize.py` was orphaned**, working and tested but with no endpoint able to call it, so 4.3.9 wasn't actually a usable deliverable (added `POST /summarize` + `routers/summarize.py` + 6 endpoint tests + `docs/api/summarize.md` so Track C has a contract for 4.3.11); and **two pieces of speculative code cut** — a trivial `iter_rows` adapter used nowhere, and `stratified_reservoir_sample()`, which went beyond what 4.3.6 asks for and would have double-corrected against `class_weighted_loss` if actually used. 200 tests passing (up from 141). Design writeup: `ai/RETRAINING.md`. Left alone: 4.3.5 (needs `UserReports` from Track A 4.3.1) and 4.3.8. Marked `[-]` not `[x]` — uncommitted. | Maxene |
| 2026-08-04 | ML | Confirmed the SHAP indicator tag dictionary (WBS 3.1.2 sign-off, `ai/PIPELINE.md` Stage 8). Reviewed all 9 tags against the manuscript and the dataset-validated rule sources, then ran a Tagalog/Taglish coverage audit — this exact class of bug (English-only lexicon missing real Filipino scam traffic) had already recurred three times (`PROMO_TL`, `JOB_SCAM`, Gambling Bait). Found it a fourth time: Unsolicited Credit Offer, Personal Info Request, and OTP/Account Phishing had zero non-English terms. Added Tagalog/Taglish keywords to all three (`ai/service/indicator_tags.py`) plus 3 regression tests (`test_*_tagalog`, `ai/tests/test_indicator_tags.py`, now 17 tests total, all passing). Dictionary contents are final for Sprint 3. | Maxene |
| 2026-08-05 | ML | PR #25 merged into `develop` (`1e660a3`) — the Sprint 4 Track B head start from the entry above is now shipped, not just committed. Flipped 4.1.2, 4.2.2, 4.2.3, 4.3.6, 4.3.7, 4.3.9, 4.4.2 from `[-]` to `[x]` in `WBS.md`. Parents 4, 4.1, 4.2, 4.3, 4.4 stay `[-]` — each still has an open Track A/C/D or all-members child. Next unblocked Track B item is 4.3.8 (campaign evolution tracking); 4.3.5 remains blocked on Track A's 4.3.1 (`UserReports`). | Maxene |
| 2026-08-05 | ML | Built campaign evolution tracking (WBS 4.3.8). Until now `scripts/cluster_campaigns.py` overwrote `campaign_clusters.json` on every re-clustering run with no history retained, so nothing could answer "how has this campaign changed since last time" — no data existed to answer it from. Closed in two parts: (1) `cluster_campaigns.py` now archives the outgoing snapshot to `datasets/processed/campaign_snapshots/campaign_clusters_<UTC-timestamp>.json` before overwriting (best-effort — an archiving failure must never block writing the actual clustering result); (2) new pure module `campaign_evolution.py` (`detect_evolution()`) compares any two snapshots and reports new campaigns, dissolved ones, continuing campaigns (growth, surge flag, newly-seen domains), merges, splits, and merge candidates (two still-distinct current clusters similar enough to plausibly be the same campaign, no history needed). Continuity between snapshots is re-derived by centroid cosine similarity rather than `cluster_id`, since HDBSCAN relabels clusters from scratch every run — reuses the same 0.85 threshold `service/campaign.py` uses for message matching, so "same campaign" means one thing everywhere rather than two. CLI: `scripts/track_campaign_evolution.py`. Design writeup: `ai/PIPELINE.md` Stage 9b. **A self-review pass before committing found three defects in the first cut, all fixed:** (a) **archived snapshots were not git-ignored** — each cluster entry carries a 160-char `sample` of real message text, and the pre-existing `ai/datasets/processed/*.json` rule matches only the top level, not the new `campaign_snapshots/` subdirectory, so a routine `git add` would have committed real personal SMS to a public repo; fixed with a recursive `ai/datasets/processed/**/*.json` rule, verified with `git check-ignore` against an actual snapshot path. (b) **The surge flag was measured on raw message counts**, but every run re-clusters the *whole* population rather than an incremental buffer, so a dataset top-up (already 15,728 → 16,772 and still growing) inflates every cluster's size at once — the flag would have fired on all 221 campaigns simultaneously, which is the same as not having a flag. Snapshots' `n_messages` is now carried through and growth is computed on each campaign's *share* of the population; raw growth is still reported for transparency but no longer drives the decision. (c) **No split detection**, though merges were handled — matching runs previous→current and each previous campaign claims only its closest fragment, so the remaining fragments of a campaign that mutated into variants fell through looking like brand-new campaigns. Since scam operators rotate templates constantly this is ordinary traffic, not an edge case, and it would have made new-campaign counts spike on every mutation; added `SplitEvent` as the dual of `MergeEvent`. 30 tests (up from the first cut's 20), 230 in the AI suite overall, all passing; also verified end to end through the CLI on synthetic snapshots where the population tripled — the flat-share campaign is correctly *not* flagged while a genuine share gain is, and a fragmented campaign reports as a split instead of a new campaign. Out of scope here (future Track A/D work): wiring a real report into the admin dashboard's Campaign Evolution Timeline (`web/src/pages/admin.tsx`, currently mock data only) and running the CLI automatically after each re-clustering pass. **Update (2026-08-09 doc-recovery pass): this work is genuinely committed as of `95a25df`, so WBS 4.3.8 is now flipped to `[x]` — the "uncommitted" note below was accurate only at time of writing.** | Maxene |
| 2026-08-04 | Docs | WBS sync: flipped 3.3.8–3.3.11 (Track C — Mobile) from Not Started to In Progress. They had been sitting as `[ ]` since Sprint 3 started, but `AlertsScreen.kt`, `SmishingAlertScreen.kt`, `CampaignsScreen.kt`, `CampaignDetailScreen.kt`, `ScamAwarenessScreen.kt`, and `TipDetailScreen.kt` are all fully built, polished, navigable Compose UIs that have existed since the Jul 3 initial commit (restyled Jul 18) — the WBS was tracking screen existence, not what's actually missing. The real gap is data-wiring: every one of these screens reads from a hardcoded `listOf(...)` (fake alerts, fake SHAP percentages, fake campaign stats/domains) instead of the backend, so there's no Retrofit/API call behind any of them. 3.3.10 specifically maps to one tabbed `NotificationsScreen.kt` (Threat alerts / Weekly digest) rather than four separate files, which is likely why it never got flagged as started. Confirmed 3.2.3 (mobile UX spec doc — no such doc exists anywhere in `docs/`) and 3.4.3 (SHAP integration test — no test files exist under `mobile/`, and `SmsApi.kt`'s `IngestResult` doesn't even parse a SHAP field from the backend response) are genuinely unstarted, left as `[ ]`. No code changed. | Gio |
| 2026-08-04 | Android | Wired the Campaigns screens (WBS 3.3.9, Active half) to the real backend instead of a hardcoded list. New `CampaignsApi.kt` (plain `HttpURLConnection` + `org.json`, matching `AuthApi.kt`/`SmsApi.kt` — no Retrofit in this codebase) adds a `get()` helper alongside the existing `post()` pattern, calling `GET /campaigns` and `GET /campaigns/:id`. New `CampaignsViewModel`/`CampaignDetailViewModel` (`AndroidViewModel`, `StateFlow`, token read from `UserPreferences`) back `CampaignsScreen.kt` and `CampaignDetailScreen.kt`. `CampaignDetailScreen` previously only received an `isActive: Boolean` nav arg — the same two fake campaigns regardless of which of the 5 mock list rows was tapped — so `Screen.CampaignDetail` in `NavGraph.kt` now takes a real `campaignId: String`, mirroring the existing `SuspiciousDetail`/`SettingsTipDetail` string-arg pattern. Detail screen stats are now honestly scoped to what the API actually returns: Messages/Domains are real totals, but the old "Affected"/"Reports" cards (no `Alert`/reporting backend exists) were replaced with **Senders**/**Blocked** computed from the up-to-50 most recent messages the endpoint returns, labeled "(recent)" rather than implying a lifetime total; the SHAP-derived "Message patterns" section (no GET endpoint exists for indicators) was replaced with a **Recent messages** list using real sender/body/label data already present in the same response. **Scoped mobile-only, deliberately**: `GET /campaigns` hardcodes `where: { isActive: true }` server-side with no way to list inactive clusters, so rather than touch `backend/src/campaigns/` (Reymark's track), the Past Campaigns section and `InactiveCampaignModal` were replaced with an honest "not available yet" state instead of staying on fake data. Two other mock screens (`SmishingAlertScreen.kt`, `ThreatAnalysisScreen.kt`) called the old `CampaignDetail.createRoute(true)` on a campaign chip with no real ID behind it; removed the click-through on both rather than route to a fabricated ID, since neither screen is wired this round. `compileDebugKotlin` passes (`BUILD SUCCESSFUL`, ran via Android Studio's bundled JBR since the system `java` on PATH wasn't a real JDK). Not committed. | Gio |
| 2026-08-05 | Android | Onboarding: `OnboardingConfirmNumberScreen.kt` had the phone number hardcoded to a fake placeholder while claiming "detected from your SIM" — no code anywhere called `TelephonyManager`. Now requests `READ_PHONE_NUMBERS` at runtime and reads `TelephonyManager.line1Number`, normalizing whatever format the carrier returns into `+63 9XX XXX XXXX`; falls back to a blank manually-editable field (and honest subtitle copy) when the carrier doesn't expose the number or the permission is denied, since PH carriers frequently leave this empty regardless of permission. | Gio |
| 2026-08-05 | Android | Notification tap deep-linking: `NotificationHelper` set a `navigate_to` intent extra on every alert notification, but nothing ever read it, so tapping a smishing/suspicious alert just opened the app to its default screen. `MainActivity` now resolves the extra (cold start via `onCreate` and already-running via `onNewIntent`) into a requested tab index threaded through `NavGraph` → `MainScreen`, which jumps `selectedTab` via `LaunchedEffect`. | Gio |
| 2026-08-05 | Android | Fixed three real bugs surfaced by on-device testing on the connected Huawei VOG-L29 (WBS 2.3.7 follow-up — the receiver was marked complete but had never been exercised against a real second phone sending real texts). (1) **Duplicate messages on every receive** ("double hello" both in the thread and the inbox preview): `SmsReceiver`'s manifest intent-filter registered both `SMS_DELIVER` and `SMS_RECEIVED` — Android delivers `SMS_DELIVER` exclusively to the default SMS app *and* separately broadcasts `SMS_RECEIVED` to every app holding `READ_SMS` (which includes the default app), so the same receiver fired twice per incoming text and inserted two rows. Fixed by registering `SMS_DELIVER` only, matching how every real default-SMS-app reference implementation does it. (2) **No live update while a thread/list was open**: `HomeViewModel`, `MessagesViewModel`, and `MessageDetailViewModel` all did one-shot provider queries with no way to learn about new rows; added a `ContentObserver` on `Telephony.Sms.CONTENT_URI` to each (registered in `init`, unregistered in `onCleared`) so an open thread or the inbox list now refreshes itself the moment a new message lands. (3) **Ordinary messages never notified at all**: `SmsReceiver` explicitly skipped notifying for the `INBOX`/`"safe"` classification ("stored silently"), which is wrong for an app standing in as the user's default texting app — added a new `bantai_messages` channel and `NotificationHelper.sendMessageNotification()`, wired into both the backend-classified and local-fallback paths, tapping into the Messages tab (new `TARGET_MESSAGES` deep-link value). `compileDebugKotlin` passes; rebuilt, reinstalled, and reverified on the connected device. Residual, not a code bug: this device is Huawei/EMUI, which is known to throttle background broadcast-receiver work and suppress notifications for apps not whitelisted in its Battery/Protected-apps settings — flagged to the user as a likely contributor to perceived notification delay separate from the three fixes above. | Gio |
| 2026-08-05 | Android | Conversation grouping: the Messages list and Home tab's recent-messages preview rendered one row per individual SMS row from the provider, so a sender with 3 texts showed as 3 separate rows instead of one conversation. New `SmsConversations.kt` (`List<SmsMessage>.groupedBySenderLatest()`) collapses to one row per normalized sender address — the latest message as the preview, marked unread if any message in that sender's group is unread — applied in `MessagesViewModel` (all filters except Recently Deleted, which stays flat/ungrouped) and `HomeViewModel`. | Gio |
| 2026-08-05 | Android | Message deletion (new feature, scoped and confirmed with the user before building: whole-conversation delete from the Messages list *and* individual-message delete inside an open thread, both with restore support, manual-purge-only for now). Android's SMS provider has no trash concept and only the default SMS app may touch it at all, so deletion is two-stage and app-local: new `DeletedMessagesStore.kt` (dedicated DataStore file, JSON-serialized id+timestamp list — no Room added, not worth the dependency for this dataset size) tracks soft-deleted message ids; `SmsRepository` gained `getMessagesByIds()` (backs the Recently Deleted view, queries the full table so a deleted conversation's outgoing replies show too) and `deletePermanently()` (real, irreversible `ContentResolver.delete()` — only reachable from the second confirmation). `MessagesViewModel` filters soft-deleted ids out of Messages/Spam/Blocked/Unread, populates `RECENTLY_DELETED` (initially flat/ungrouped, corrected same day — see next entry) from the store, and adds selection-mode state (`enterSelectionMode`/`toggleSelected`/`selectAll`/`deleteSelected` — which expands each selected conversation-preview row back out to every message id currently in that sender's thread — `restoreSelected`/`permanentlyDeleteSelected`). `MessageDetailViewModel` gets its own selection state for individual in-thread messages (no expansion — deletes exactly the selected ids) and now filters soft-deleted ids out of `loadConversation()`. UI: `MessagesScreen.kt` and `MessageDetailScreen.kt` both add long-press-to-select (`combinedClickable`), a selection top bar (Cancel / N selected / Select All / Delete, or Restore + Delete Permanently in Recently Deleted), a `BackHandler` that exits selection mode instead of navigating back, and two `AlertDialog` confirmations (soft-delete: "they'll move to Recently Deleted"; permanent delete: "this can't be undone") per the user's explicit ask for a confirmation pop-up before either. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device. Not committed. | Gio |
| 2026-08-05 | Android | Recently Deleted grouping fix, caught by the user on first test: it had deliberately been left flat/ungrouped ("actions target specific rows" reasoning), but that broke the same conversation-grouping convention just added everywhere else — deleting one 5-message conversation showed as 5 separate same-sender rows instead of one. Exposed `normalizeSenderKey()` from `SmsConversations.kt` (was private) and now group Recently Deleted with the same `groupedBySenderLatest()` as every other filter. Since a grouped row's id is only its latest deleted message, `restoreSelected()`/`permanentlyDeleteSelected()` needed the same expansion `deleteSelected()` already does — new `expandDeletedSelectionToSenders()` resolves a selected row to every currently-soft-deleted id for that sender (sourced from `_recentlyDeleted`, the flat backing list) before restoring/permanently deleting. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Two more real bugs found while verifying send/compose end to end (confirmed BantAI genuinely holds `android.app.role.SMS` via `dumpsys role` — a stale `Settings.Secure.sms_default_application` read had briefly suggested otherwise, but that key is legacy and not what `Telephony.Sms.getDefaultSmsPackage()` actually reads on this OS version). (1) **Sent messages never appeared anywhere**: `ComposeScreen.kt` and the reply bar in `MessageDetailScreen.kt` both call `SmsManager.sendTextMessage()`, which transmits the SMS but — contrary to a common assumption — does not write a local record even for the default SMS app; the sender has to insert it themselves. Added `SmsRepository.insertSentMessage()` (mirrors `SmsReceiver`'s `storeMessage()` but as `MESSAGE_TYPE_SENT`) and call it right after a successful send in both screens, so a sent reply now actually shows up as an outgoing bubble instead of silently vanishing. (2) **Back gesture from a non-default filter exited the tab instead of returning to Messages**: reported specifically from Recently Deleted, but affected every non-default `MessageFilter` (Spam/Blocked/Unread too) — filters are plain state, not nav-stack entries, so back had nothing to pop to and fell through to leaving the screen. `MessagesScreen.kt`'s `BackHandler` now also triggers when `selectedFilter != MESSAGES`, resetting to Messages before falling through. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Drafts filter (WBS 4.3.10 overlap — Compose Message screen — though Drafts itself wasn't separately tracked): previously a dead stub (`MessageFilter.DRAFTS -> emptyList()`), and unsent Compose text was silently discarded on leaving the screen. Unlike Inbox/Sent/soft-deleted, the SMS provider has no draft concept at all, so this is entirely app-local: new `DraftsStore.kt` (same DataStore-file pattern as `DeletedMessagesStore`, one draft per recipient keyed by `normalizeSenderKey()`, a blank body clears rather than stores an empty draft). `Screen.Compose` gained optional `recipient`/`body` query args (`compose?recipient={recipient}&body={body}`) so a draft can be resumed; every existing `navigate(Screen.Compose.route)` call site updated to `Screen.Compose.createRoute()` since the raw pattern string is no longer a directly-navigable route. New `ComposeViewModel` (`saveDraft`/`clearDraft`) — deliberately viewModelScope-backed rather than a screen-level `rememberCoroutineScope()`, since the save has to fire from `DisposableEffect`'s `onDispose` as the screen is leaving, and a scope tied to that same composable could be mid-cancellation at that exact moment; the ViewModel's scope (tied to the NavBackStackEntry) reliably outlives it. `ComposeScreen` now saves a draft on dispose unless a `justSent` flag was just set (successful send clears the draft instead). `MessagesViewModel` collects `DraftsStore.drafts` directly (DataStore's Flow already pushes on every write, so this is live without any polling) and synthesizes each entry into a display row — no real provider row exists so `id` is a hash of the recipient address, safe since drafts and real messages never share a visible list. Tapping a draft in `MessagesScreen.kt` opens Compose pre-filled instead of a conversation thread (a draft may have no real messages yet); preview text gets a "Draft: " prefix; long-press selection is disabled for this filter since the existing delete/restore flow is built around real message ids that drafts don't have — discarding a draft is done by opening and clearing it. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Three more issues from live device testing against a second phone. (1) **Reply box hidden behind the keyboard in an open thread**: `MessageDetailScreen.kt`'s root `Column` was missing `.imePadding()` entirely — `ComposeScreen.kt` already had it, this screen never did, so the keyboard just covered the reply bar with no way to see the send button or scroll it into view. (2) **No feedback on whether a send actually worked**: both `ComposeScreen` and the reply bar called `SmsManager.sendTextMessage(..., sentIntent = null, ...)` — a `null` sentIntent means the app never learns whether the carrier actually accepted the SMS; the call returning just means "handed to the radio," so a no-signal or airplane-mode failure looked identical to success. New `SmsSender.kt` wraps send with a real `PendingIntent`-based result callback (`RESULT_ERROR_NO_SERVICE`, `RESULT_ERROR_RADIO_OFF`, etc. each map to a real error Toast now); both screens use it in place of calling `SmsManager` directly. Documented a real limitation rather than overclaiming: a prepaid SIM with no load is usually rejected at the carrier's SMSC, invisible to the sending device, so that specific case can still report success — the fix catches radio/signal-level failures, not billing failures. (3) **Replying inside an existing thread didn't save a draft**: only `ComposeScreen`'s new-message flow had draft support from the same-day earlier entry; the in-thread reply bar had none. `MessageDetailViewModel` gained `draftBody` (loaded per-sender in `loadConversation()`), `saveDraft()`/`clearDraft()` alongside its existing `DeletedMessagesStore` usage; `MessageDetailScreen` now pre-fills the reply box from any existing draft on open and saves on `DisposableEffect`'s `onDispose` unconditionally — simpler than `ComposeScreen`'s `justSent`-flag approach and correct without one, since a successful send already clears `replyText` to `""` before the screen could be left, and an empty body already means "clear the draft" in `DraftsStore`. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Two more real bugs from continued live testing. (1) **Send button appeared to do nothing**: root cause was `SmsSender.send()` (same-day earlier entry) having no bound on how long it waits for the carrier's `sentIntent` callback — on a real device in a degraded radio state the callback can simply never fire, leaving `isSending` stuck `true` forever with no error, which looks identical to a dead button. Added a 20s timeout (`Handler.postDelayed`, guarded by an `AtomicBoolean` so a late real callback can't double-fire after the timeout already resolved it) that falls back to a failure result. Also restructured both `ComposeScreen` and the reply bar around Android's actual Outbox/Sent/Failed message types (`Telephony.Sms.MESSAGE_TYPE_OUTBOX`/`SENT`/`FAILED` — real, already-existing provider concepts, not a custom status field) instead of waiting on the send result before showing anything: `SmsRepository.insertSentMessage()` replaced with `insertOutgoingMessage()` (inserts as Outbox immediately, returns the row id) + `updateMessageType()` (resolves that same row to Sent or Failed once `SmsSender`'s callback fires). The message now appears in the thread the instant you hit send — matching the iPhone-style "shows immediately, resolves to sent or failed" behavior asked for — with a "Sending…" caption while pending and a red "Not delivered · Tap to retry" row under a failed bubble (`retryFailedMessage()` re-flips the same row back to Outbox and re-sends, rather than creating a duplicate). `SmsMessage` gained a `sendStatus: SendStatus` field (`NONE`/`SENDING`/`SENT`/`FAILED`) populated from the real `TYPE` column in `getConversationBySender()`. (2) **A real, non-scam message from a real test sender showed the Suspicious badge**: root cause was structural, not a keyword tuning issue — `SmsRepository`'s read methods (`getInboxMessages`, `getConversationBySender`, etc.) unconditionally re-derive every message's classification via the local keyword heuristic on every read, regardless of what the real backend/AI model actually decided at receive time; the backend's classification is used once (for the receive-time notify/block decision) and then discarded, because the stock Android SMS schema has no column to persist it. New `ClassificationStore.kt` (same DataStore pattern as `DeletedMessagesStore`/`DraftsStore`, message id → classification string) closes that gap: `SmsReceiver.storeMessage()` now returns the inserted row's real id (via `ContentUris.parseId`), threaded through `classifyAndNotify()` into a new `persistClassification()` called from both `applyBackendAction()` (`BLOCKED`→"suspicious", `ALERT`→"unknown", `INBOX`→"safe" — chosen to match the existing local-heuristic severity mapping so no other filtering logic needed to change) and `applyLocalClassification()` (persists whatever the heuristic actually returned, for consistency when running in fallback mode). `SmsRepository`'s read methods now do `stored[id] ?: classifyMessage(...)`, preferring the real recorded verdict and only guessing for messages that predate this fix. Read methods stayed non-suspend (fetching the stored map via a single `runBlocking` per call, acceptable since every caller already runs on `Dispatchers.IO`) rather than threading `suspend` through the whole repository and every ViewModel call site. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Tap-outside-to-dismiss-keyboard in an open thread: `MessageDetailScreen.kt`'s message `LazyColumn` had no gesture for this, so the only way to close the keyboard was the system back button. Added `Modifier.pointerInput(Unit) { detectTapGestures(onPress = { focusManager.clearFocus(); keyboardController?.hide() }) }` on the list — scoped to the thread background specifically (a sibling of the reply `BasicTextField`, not a wrapper around it), so it doesn't fight the reply field's own tap-to-focus or a message row's `combinedClickable`. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Android | Two more real gaps flagged by the user. (1) **Read/unread never actually persisted**: opening a thread never wrote back to the provider's own `READ`/`SEEN` columns, so the Messages list kept showing the unread dot after visiting a conversation — it wasn't a UI-state bug, nothing was updating the database at all. New `SmsRepository.markConversationRead()` (`WHERE READ = 0`, so re-marking an already-read thread touches 0 rows) called once from `MessageDetailScreen`'s `LaunchedEffect(sender)` — deliberately *not* from `MessageDetailViewModel`'s existing `ContentObserver` reload path, since that update would itself trigger the observer and risked a refresh loop; calling it once per screen visit sidesteps that entirely while still converging (the observer picks up the resulting change and reloads once, harmlessly). (2) **No real-time notification when a send fails**: a failed send only showed a `Toast` (foreground-only, gone if you'd already navigated away — easy to miss given results can resolve after the 20s timeout from the same-day earlier entry) and updated the in-thread bubble, which you'd only see by reopening that specific thread. Added `NotificationHelper.sendFailedMessageNotification()` (new `bantai_send_status` channel) fired from all three failure paths (`ComposeScreen`, the reply bar, `retryFailedMessage()`). Tapping it deep-links straight into the specific failed conversation rather than just the Messages tab — extended the existing notification-tap-routing mechanism (`MainActivity` → `NavGraph`) with a new `EXTRA_CONVERSATION_SENDER` intent extra and a `LaunchedEffect` in `NavGraph` that calls `navController.navigate(Screen.Detail.createRoute(sender))` once the graph is up, alongside the existing tab-index deep-link. `compileDebugKotlin` passes; rebuilt and reinstalled on the connected device, not yet manually walked through by the user. Not committed. | Gio |
| 2026-08-05 | Backend | Sprint 3 unit tests: wrote `verification.service.spec.ts` (20 tests covering all three methods — verifySender contact/cache/unknown paths, syncContacts upsert and normalization, reportFraud with 30-day TTL, and normalizePhone edge cases). Also resolved a merge conflict in `sms/dto/store-indicators.dto.ts` that was blocking the full test suite. All 97 backend tests now passing. | Reymark De Castro |
| 2026-08-05 | Backend | Sprint 3 backend completion: added three endpoints blocking mobile wiring — `GET /api/campaigns/inactive` (inactive clusters for the Past Campaigns tab), `GET /api/sms/alerts` (user alert history with message + classification data), `GET /api/sms/:messageId/indicators` (SHAP tags per message; empty array is valid while SHAP computes). Fixed a core gap: `POST /api/sms/ingest` never wrote to the Alert table, so `getAlerts` would always return empty — ingest now creates a Pending alert for routed-to-alert messages and a Blocked alert for auto-blocked ones; inbox messages get no alert row. All endpoints JWT-guarded; indicators verifies message ownership. 115/115 tests passing. | Reymark De Castro |
| 2026-08-05 | Backend | Security hardening, bug fixes, and API quality pass (branch `feature/backend-sprint3-verification-tests`). Security: added `helmet` (12 HTTP security headers), `@nestjs/throttler` (global 120 req/min; auth endpoints 5–10/min; fraud report 20/min), `ApiKeyGuard` with `INTERNAL_API_KEY` env var protecting all internal AI/ML endpoints (centroids, create cluster, add domains, store indicators), `crypto.randomInt()` replacing `Math.random()` for OTP generation, JWT payload stripped to `{ sub }` only (phone was PII in a decodable token), `register()` returns neutral message regardless of phone existence (anti-enumeration), unified `verifyOtp()` error so expired and wrong codes are indistinguishable, `JWT_SECRET` missing at startup now throws. Bugs fixed: sender normalization applied once at top of `ingest()` so `+639X` and `09X` formats resolve to the same blocked-number record; `deactivate()` and `updateMe()` now return 404 instead of unhandled Prisma P2025 crash; `console.log(OTP + phone)` replaced with `Logger.warn()`. API quality: `POST /auth/register`, `POST /sms/ingest`, `POST /campaigns` return 201; `getMe()` uses `select` for profile fields only; `AddDomainsDto`/`SyncContactsDto`/`IngestSmsDto`/`StoreIndicatorsDto`/`RegisterDto`/`VerifyOtpDto` all tightened with size and format validators; `.env.example` updated with `INTERNAL_API_KEY` and `AI_SERVICE_URL`. Tests: 120 passing across 16 suites (up from 115). | Reymark De Castro |
| 2026-08-05 | Android | Finished the two WBS 3.3.8/3.3.9 items that were blocked on Reymark's new endpoints. **Campaigns Inactive tab (3.3.9)**: added `CampaignsApi.listInactive()` (`GET /campaigns/inactive`, same response shape as the active endpoint) and mirrored `CampaignsViewModel`'s active-campaign loading state for inactive; `CampaignsScreen.kt`'s "Past campaigns aren't available yet" placeholder is now a real `GroupedList` like Active. **Alerts (3.3.8)**: `AlertsScreen.kt`'s hardcoded three-item list is replaced with `AlertsViewModel` backed by new `SmsApi.getAlerts()`. The bigger gap was the two detail screens (`SmishingAlertScreen.kt`, `ThreatAnalysisScreen.kt`): both took zero navigation arguments and were 100% hardcoded to one demo GCash message — `Screen.SmishingAlert` now takes a required `messageId` path arg (mirroring `CampaignDetail`), and `Screen.ThreatAnalysis` takes an *optional* `messageId` query arg instead, since `MessageDetailScreen.kt`/`SuspiciousDetailScreen.kt` already navigate there from an AI-summary shortcut/suspicious-thread banner with no specific message tracked — those 4 call sites were updated to `Screen.ThreatAnalysis.createRoute()` (matching the precedent from Compose's optional-arg route). New `AlertDetailViewModel` backs both screens: no single-alert-by-id endpoint exists yet, so it re-fetches `GET /sms/alerts` and finds the matching `messageId` client-side, plus `GET /sms/:messageId/indicators` for the SHAP bars (indicator failure is non-fatal — empty list just means no bars render); a blank messageId (the two legacy entry points) short-circuits to an honest "no details available" state instead of guessing. Known gap, deliberately not faked: the "Part of: [campaign name]" line both detail screens used to show has no backend source — `SmsMessage.clusterId`/`cluster` exists in the schema, but `getAlerts()`'s Prisma `select` doesn't return it — so that section was dropped rather than left showing the old hardcoded campaign name. Also replaced the fabricated SHAP narrative/AI-summary prose on both screens with a factual one-liner generated from the real `label`/`score`. `compileDebugKotlin` passes (`BUILD SUCCESSFUL` via Android Studio's bundled JBR — `./gradlew` needed `JAVA_HOME` pointed at it explicitly and only ran through `gradlew.bat`, not the bash wrapper, in this environment). Not committed. | Gio |
| 2026-08-07 | Docs | Created `docs/api/mobile-alerts-campaigns-ux.md` (WBS 3.2.3) — full UX spec covering screen hierarchy, data sources, states, and navigation contracts for `AlertsScreen`, `SmishingAlertScreen`, `ThreatAnalysisScreen`, `CampaignsScreen`, and `CampaignDetailScreen`. Serves as the design record for Sprint 3 Track C. | Gio |
| 2026-08-07 | Android | Wired `ThreatAnalysisScreen` (WBS 3.3.8) to real backend data. Rewrote the screen from a hardcoded suspicious-message mock to a fully parameterized composable accepting `messageId`, `sender`, `score`, `status`, and `body` from the `AlertsScreen` navigation call. Reuses `SmishingAlertViewModel` and `GET /sms/:messageId/indicators` (JWT) to show live SHAP indicator bars in amber (Suspicious color). Updated `Screen.ThreatAnalysis` in `NavGraph.kt` to the same 5-param URL pattern as `SmishingAlert`; updated `AlertsScreen.kt` to pass real values for Pending-status alerts. | Gio |
| 2026-08-07 | Android | Wired the Past Campaigns tab (WBS 3.3.9) now that `GET /api/campaigns/inactive` exists on the backend. Added `listInactive()` to `CampaignsApi.kt`; added `_inactiveCampaigns`, `_isLoadingInactive`, `_inactiveError` StateFlows to `CampaignsViewModel` with a parallel `coroutineScope { launch{} launch{} }` fetch; updated `CampaignsScreen.kt` PAST CAMPAIGNS section to use the real loading/error/empty/data pattern matching the ACTIVE section. | Gio |
| 2026-08-07 | Android | Wired `NotificationsScreen` Threat Alerts preview to real alert history (WBS 3.3.10). Added `_recentAlerts` and `_alertsLoading` StateFlows to `SettingsViewModel`; on init, fetches `GET /sms/alerts` (JWT) and stores the result. `ThreatAlertsTab` now shows up to 3 most-recent real alerts as notification preview items — Blocked alerts render as "⚠ Smishing detected" with score and "Auto-blocked" text, Pending as "⚡ Suspicious" with "Review in BantAI". Loading shows a spinner; no alerts shows a "No threats detected yet" fallback item. Relative time computed from `createdAt` ISO field. | Gio |
| 2026-08-07 | Android | Wired `ScamAwarenessScreen` tips to the user's real threat history (WBS 3.3.11). New `ScamAwarenessViewModel` fetches `GET /sms/alerts` (JWT) on init and derives a `relevantTipIds: Set<String>` by mapping alert labels/buckets to tip IDs (`scam`/`blocked` → `gcash`, `otp`, `links`; `spam`/`Pending` → `urgency`, `links`; any match adds `action`). `ScamAwarenessScreen` now accepts the ViewModel via default parameter (`= viewModel()`); tips whose ID is in `relevantTipIds` display an amber "Relevant to you" badge inline with the category label. No change to NavGraph call site. | Gio |
| 2026-08-07 | Backend | Added `backend/test/shap-indicators.e2e-spec.ts` (WBS 3.4.3) — 8 e2e tests verifying the full async SHAP pipeline end-to-end: (1) `GET /sms/:id/indicators` returns `{ indicators: [] }` immediately after ingest; (2) `POST /sms/:id/indicators` with API key stores indicators; (3) wrong key or JWT instead of API key → 401; (4) `GET` after `POST` returns stored indicators sorted most-influential-first; (5) second `POST` upserts (overwrites first set); (6) `GET` without JWT → 401; (7) nonexistent messageId → 404. `AiService` is overridden so the suite is hermetic — Python ML service not required. | Gio |
| 2026-08-05 | Dashboard | UI styling refinement (commit `b308548`): reworked `Topbar.tsx`, restructured large portions of `admin.tsx` and `client.tsx` header/card layout, and added transition styling in `components.css`. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Daryl De Castro |
| 2026-08-07 | Docs | WBS sync + Sprint 3 mobile↔backend verification: flipped 3.3.8–3.3.11 from In Progress to Complete — both had actually landed on `develop` (PR #33 `feature/sprint3-mobile-wiring` + PR #34 `feature/alerts-campaigns-wiring`) but the WBS still read "not yet committed"/"hardcoded". Confirmed by reading the merged code: Alerts, Campaign (active+inactive), Smishing/Threat detail, and Notifications' Threat-alerts tab all call real endpoints (`GET /sms/alerts`, `GET /sms/:messageId/indicators`, `GET /campaigns`, `GET /campaigns/inactive`, `GET /campaigns/:id`); `ScamAwarenessViewModel` derives tip relevance from real alert data. Only intentionally-static piece left is the Weekly Digest tab preview, which has no backend feature behind it (not a Sprint 3 scope item). Backend: local `node_modules` was stale post-pull (`@nestjs/throttler` in `package.json` but not installed, failing `auth.controller.spec.ts`) — ran `npm install`, full suite now 120/120 passing. Known deliberate gap, unchanged: alert detail screens don't show campaign membership since `GET /sms/alerts` doesn't select `clusterId`. No app code changed. | Gio |
| 2026-08-07 | Docs | Full WBS re-audit against actual code, Sprints 2–5, ahead of Sprint 4's review window closing today. **Backend Track A (4.1.1, 4.2.1, 4.3.1–4.3.4, 4.4.1, 4.4.3)**: confirmed genuinely not started — `reports.controller.ts`/`reports.service.ts` are still empty Nest scaffolds, no `UserReport`/`ModelVersion` Prisma models or migrations exist, no `@nestjs/schedule`/cron anywhere in `backend/src`; `ai/retraining/triggers.py` already implements the trigger *policy* in Python and explicitly documents that it's waiting on this NestJS cron. WBS left unchanged here — it was already accurate. **Mobile Track C (4.3.10–4.3.12)**: flipped `[ ]`→`[-]`, each is half-real. `ComposeScreen.kt` (send/drafts/outbox) is fully done but no "Unknown" message filter exists (4.3.10). `AISummaryBottomSheet.kt` is wired to a real suspicious/unknown trigger but its verdict text and confidence % are still 100% hardcoded — no mobile `SummarizeApi.kt` and no backend proxy for the AI service's existing `POST /summarize` (4.3.11). `BlockedNumbersScreen.kt`/`ViewModel.kt` are fully built but purely local via Android's `BlockedNumberContract`, never synced to the backend's `BlockedNumber` table (4.3.12). **Web Track D (4.3.13–4.3.15)**: flipped `[ ]`→`[x]`. All 11 named admin pages already exist as full components inside `web/src/pages/admin.tsx` and are routed in `AppRoutes.tsx` — same mock-data-only status as the already-`[x]` 3.3.12/3.3.13 pages, consistent with CLAUDE.md's standing note that web↔backend wiring is a distinct, not-yet-scheduled effort. Confirmed no `fetch`/`axios`/API base URL exists anywhere in `web/src` — that project-wide gap is unchanged. Left alone, flagged but not changed: Sprint 2's 2.1.1 (still `[-]`) and 2.5 (Sprint 2 demo/retro, still `[ ]`) — these are analysis/ceremony items with no code artifact to verify against; need a human call, not a code check. No app code changed. | Gio |
| 2026-08-07 | Docs | Closed the two flagged Sprint 2 ceremony items on Gio's confirmation that Sprint 2's demo/retro and testing were conducted: flipped `2.1.1`, `2.5`, `2.5.1`, `2.5.2` to Complete in `WBS.md` — Sprint 2 (`2`) is now fully `[x]`. Also fixed a standing inconsistency while re-checking Sprint 5: `5` and `5.3` were still `[ ]` even though `5.3.2` (API security/rate limiting) was already `[x]` — flipped both to `[-]` In Progress per WBS rule 1. Propagated the full current state (Sprints 1–3 complete, Sprint 4/5 partial per the two prior audit entries) into `docs/development/BantAI_WBS.xlsx` (Task Status column, Actual Completion Date backfilled to 2026-08-07 for newly-flipped rows, header date bumped) and `docs/development/BantAI_Sprint_Schedule.xlsx` (Sprint Summary status column, Gantt sprint-header labels, Sprint Backlog per-row status — 25 rows updated, several descriptions rewritten to drop now-false "hardcoded"/"not started" language). Sprint Schedule's original 3-week-cadence dates were deliberately left untouched — they read as the originally-proposed plan, distinct from the WBS's actual-execution dates, and only Status was in scope. One honesty caveat kept in the record rather than silently smoothed over: Sprint Backlog rows 6–7 (OpenAPI/Swagger docs, a dedicated `Protection_Settings` table) are marked Done to match `WBS.md`'s existing bar for 1.3.4/1.3.5, but neither actually exists in code — Swagger was substituted with manual `docs/api/*.md` files instead. Also removed `docs/development/~$BantAI_Sprint_Schedule.xlsx` from git — an Excel lock/temp file, auto-created while the real file was open and saving, accidentally committed alongside it — and added `~$*.xlsx`/`.xls`/`.docx`/`.doc`/`.pptx` to `.gitignore` to stop it recurring. | Gio |
| 2026-08-07 | Android | Fixed real compile-time damage from the PR #33/#34 merge (`2e5cf6e`, `82af44a`), caught while Gio was doing a live device test build (`Conflicting overloads: getAlerts`). The two branches had independently added near-identical alerts/indicators plumbing, and the merge silently concatenated both instead of reconciling them — Kotlin only flagged the parts with truly identical signatures, so the rest sat as dead/broken code until a real `compileDebugKotlin` ran. Fixed four files: **`SmsApi.kt`** had two full parallel implementations (`AlertSummary`/`IndicatorTag` + a private `get()`, and a second `AlertItem`/`AlertMessage`/`Indicator` + its own `get()`) — kept the flat `AlertSummary`/`IndicatorTag` shape (used by `AlertsViewModel`/`AlertDetailViewModel`/`AlertsScreen`/`SmishingAlertScreen`/`ThreatAnalysisScreen`, the larger and more current call-site set) and deleted the nested `AlertItem` shape entirely, updating its 3 real call sites (`ScamAwarenessViewModel`, `SettingsViewModel`, `NotificationsScreen`) to the flat shape's field names (`alert.label` not `alert.message.label`, etc). Deleted `SmishingAlertViewModel.kt` outright — Reymark's original version, fully superseded by Gio's later `AlertDetailViewModel`, left orphaned (zero references anywhere) after the merge rather than removed. **`AlertsScreen.kt`** had two `when`-branches building `AlertCard` two different ways (one matching the real single-arg `Screen.SmishingAlert.createRoute(messageId)`/`Screen.ThreatAnalysis.createRoute(messageId)` in `NavGraph.kt`, one calling a 5-arg `createRoute` overload that was never real) plus a duplicated `formatAlertTime` — kept the branch that actually matches `NavGraph.kt`. **`ThreatAnalysisScreen.kt`** was missing the `Danger` theme-color import (plain oversight, not a duplication). **`CampaignsViewModel.kt`** had `loadCampaigns()` referencing an undeclared `_inactiveError` (should've been the actually-declared `_inactiveErrorMessage`) alongside a redundant `loadInactiveCampaigns()` function that duplicated `loadCampaigns()`'s parallel-fetch work and was called from `init` for no reason — fixed the property reference, deleted the redundant function per the parallel-fetch design already documented in `docs/api/mobile-alerts-campaigns-ux.md`. Verified with a real `compileDebugKotlin` via Android Studio's bundled JBR (`JAVA_HOME` pointed at `Android Studio/jbr`, run through `gradlew.bat`) — `BUILD SUCCESSFUL`. Not committed. | Gio |
| 2026-08-07 | Android/Backend | Diagnosed a login bug reported as "invalid or expired OTP" that persisted even on a fresh, unexpired code — confirmed via direct DB query that the code was in fact accepted (`OtpCode.verified` flipped `true`, matching `verifyOtp`'s own update step) so the failure was client-side, not a real rejection. Root cause: `docs/api/auth.md` documents `POST /auth/verify-otp` as returning a `user` object, and `AuthApi.kt`'s `verifyOtp()` parser required `json.getJSONObject("user")` to build its result — but the backend has never actually returned one; `auth.service.spec.ts` explicitly asserts `user` is absent by design ("no PII in the auth payload," from the `bf93e23` security-hardening pass). So every successful verification was throwing a `JSONException` inside `mapCatching` before the token ever reached `UserPreferences`, surfacing to the user as a login failure indistinguishable from a real invalid/expired code. Considered adding `user` back to the backend response (matches the doc) but that would reverse a deliberate, tested hardening decision — fixed the actual mismatch instead: trimmed `AuthApi.AuthResult` to just `accessToken` (the only field any caller used — `userId` was already dead, and `phone` was redundant with `OnboardingViewModel`'s own already-known `current.phoneNumber`), updated the one call site, and corrected `docs/api/auth.md` to document the real response shape. Also fixed a second, unrelated doc/reality gap on the same page while diagnosing this (source of the user's "before it showed the OTP in terminal" confusion): the doc claimed OTPs print to the backend console, but `otp-sms.service.ts`'s actual no-key warning never includes the code — corrected the doc to say to read it from the `OtpCode` table instead. Verified: `auth.service.spec.ts` (13/13, unchanged since the backend code reverted to its original tested form) and a real `compileDebugKotlin` both pass. Not committed. | Gio |
| 2026-08-07 | Backend | Closed remaining Sprint 2 & 3 backend gaps (commit `07b6558`). Real OTP delivery: new `OtpSmsService` sends codes via the Semaphore PH SMS gateway (`SEMAPHORE_API_KEY` env var; logs a warning and no-ops in dev when the key is absent, so local testing still works without a provider) — wired into `AuthService`, replacing the `console.log`/TODO placeholder and closing the "Integrate a real SMS provider for OTP delivery" item from the Iteration 2 backlog. `VerificationService.syncContacts()` batched into a single Prisma `$transaction` instead of sequential upserts (large contact-list imports no longer make N round trips). Minor DTO/formatting tightening on `add-domains.dto.ts` and `store-indicators.dto.ts`. Also removed a leftover generic `nest new` scaffold e2e test (`backend/test/app.e2e-spec.ts` + `jest-e2e.json`) that had reappeared in the tree despite already being replaced by a real health-check test back on 2026-07-31 (per that day's entry) — most likely resurrected by one of the merge conflicts around this time. *(Entry backfilled 2026-08-09 from the commit diff — this was never logged at the time.)* | Reymark De Castro |
| 2026-08-09 | Docs | Recovered nine `DEV_LOG.md` entries (Maxene ×2, Reymark ×1, Gio ×6) that had been written and committed by their original authors (`95a25df`, `c392b59`) but were silently dropped from the file at some point during the messy PR #33–#37 merge sequence — the same kind of merge damage the 2026-08-07 entry above already documents for code, just never previously noticed here since prose doesn't fail to compile. Also wrote seven new entries for commits that were never logged at all (Reymark's clustering/verification/link-suppression foundations, `StoreIndicatorsDto`, `GET /campaigns/centroids`, and the Sprint 2&3 backend-gaps closeout; four Daryl web-dashboard commits; one Gio web visual-edits commit), grounded in the actual commit diffs rather than reconstructed from memory. Flipped WBS `4.3.8` (campaign evolution tracking) from `[-]` to `[x]` — genuinely committed, the "uncommitted" caveat in the recovered log entry was only accurate as of when it was originally written. | Gio |
| 2026-08-09 | Android/ML | Added a debug-only "Simulate incoming SMS" tool (Settings → Developer Tools, gated behind `BuildConfig.DEBUG`) after live carrier testing showed Philippine telcos silently drop scam-pattern SMS (shortened/suspicious links) before they reach the device — confirmed by comparing plain "ham" test messages (delivered fine) against scam-link messages (never arrived, zero telephony-layer logs on the receiving device) with the phone connected via `adb`. Extracted the store/classify/notify logic shared with a real `SMS_DELIVER` broadcast out of `SmsReceiver` into a new `SmsIngestPipeline`, so the debug tool and the real receiver can't disagree on behavior. Gave the debug tool its own longer HTTP timeout (`ApiConfig.SIMULATE_TIMEOUT_MS`, 15s vs. the receiver's 5s) — the 5s cap exists only because `SmsReceiver`'s `goAsync()` window is killed by the OS after ~10s, a constraint the debug tool (an ordinary ViewModel coroutine) doesn't have; reusing the short timeout was causing it to give up and fall back to on-device heuristic classification before the backend's response — and any real `Alert` row — came back. Also added `ai/scripts/evaluate_dataset.py`, a standalone script that calls the AI service's `POST /classify` directly (no phone, no backend, no carrier) for each row of a labeled CSV and reports accuracy plus the same safety-critical metrics as `evaluate_buckets.py` (scam-missed-as-safe rate, ham-falsely-blocked rate) — verified end to end against the real trained model. | Gio |
| 2026-08-11 | ML | Built the automated retraining pipeline (WBS 4.3.5), the last open Track B item in Sprint 4. `RETRAINING.md` had it marked "⛔ Blocked on WBS 4.3.1 (Track A)" — verified that dependency is still genuinely absent (no `UserReport` model in `schema.prisma`, `backend/src/reports/` still a 4-line stub) but found the block only covers report *ingestion*, roughly one clause of one of five stages, so the rest was built rather than left waiting. New `retraining/snapshot.py` (assembly), `retraining/reports.py` (the Track A seam), `retraining/pipeline.py` (orchestration), `scripts/retrain.py` (CLI), 57 tests. Report ingestion goes through a `ReportSource` interface with `NullReportSource` (honest default — the manifest records `null (no report store configured)` rather than implying reports were consulted and none found) and `FileReportSource` (CSV/JSONL); a `DatabaseReportSource` drops in behind the same two methods when 4.3.1 lands, with no change to the snapshot or pipeline code. Three assembly decisions where the obvious implementation is wrong, each documented at its site: **reports are never sampled away** (pooling them with history and drawing uniformly, the natural reading of "combine then reservoir-sample," would let a cap discard the very corrections that triggered the retrain — so reports go in whole and the reservoir samples only the historical side down to the remaining budget); **report labels win on collision** (keeping both copies would train on two readings of the same text and learn nothing from the correction); **de-duplication compares masked text but the snapshot stores raw text** (`...1q2w3e7.ca` and `...1q2w3e8.ca` are one model input once masked — the leakage `training/dataset.py` already guards against on the split — but writing pre-masked text would double-preprocess, so masking is a comparison key only). `max_history` defaults to no cap; sampling by default would silently discard training data. Two real bugs caught during end-to-end verification rather than by the tests: a dry run was advancing the "reports since last retrain" watermark, so a dry run would make the *next real* run silently skip every report it had merely looked at (fixed — `last_run_time` now skips manifests with `dry_run: true`); and `--since all` passes a naive `datetime.min` which raised `TypeError` against timezone-aware report timestamps, meaning one un-offset row in an export would crash the whole retrain (fixed — comparison normalizes both sides to UTC). Pipeline decides but never acts: it writes a promote/reject verdict with every number behind it to `decision.json` and stops, since swapping a checkpoint with no `ModelVersions` row (WBS 4.3.4) to roll back to is the unrecoverable step `RETRAINING.md` exists to prevent. Verified `--dry-run` end to end against the real dataset (14,928 rows after masked-text dedup; 2 reports + 4,998 history under a 5,000 cap) and full suite green at 291 tests. Also corrected `RETRAINING.md`'s "everything in `retraining/` is pure" claim, no longer true now that `reports.py` and `pipeline.py` do I/O. Left at `[-]` not `[x]`: the DB report source is still Track A's, and no real GPU fine-tune has run — training was not attempted locally (no GPU; see `ai/colab/`). Not committed. | Maxene |
| 2026-08-10 | Backend | Sprint 4 Track A complete (WBS 4.2.1, 4.3.1–4.3.4, 4.4.1). Added `UserReport` + `ModelVersion` Prisma models and migration `20260810000000_add_sprint4_user_reports_and_model_versions`. Built `ReportsModule`: `POST /api/reports` (JWT, user submits FP/FN correction), `GET /api/reports` + `GET /api/reports/pending` (ApiKey, admin listing), `PATCH /api/reports/:id/validate` + `PATCH /api/reports/:id/reject` (ApiKey, admin state transitions). Built `ModelsModule`: `POST /api/models` (register candidate version), `GET /api/models` + `GET /api/models/active` (list/current), `POST /api/models/:id/activate` (promote — deactivates current, activates target), `POST /api/models/:id/rollback` (reactivates a prior version with `isRollback=true`). Built `RetrainingModule` with `@Cron(EVERY_HOUR)` trigger: checks (1) 50 validated reports since last promotion, (2) F1 drop ≥5 pts vs best prior model, (3) Page-Hinkley drift on recent classification score stream; if any condition fires, calls `POST {AI_SERVICE_URL}/retrain`. `ScheduleModule.forRoot()` registered in `AppModule`. Unit tests: 30 new tests across `ReportsService` (14), `ReportsController` (5), `ModelsService` (8), `ModelsController` (5). All 152 tests passing (up from 120). | Reymark De Castro |
| 2026-08-16 | Docs | WBS/tracker re-sync ahead of a Sprint 4 status check. PR #39 (`2168c28`, Sprint 4 Track A — `UserReport`/`ModelVersion` models, `ReportsModule`, `ModelsModule`, hourly retraining cron) merged to `develop` 2026-08-12 but `WBS.md` was never flipped: `4.3.1`–`4.3.4` still read Not Started and parent `4.2` was stuck In Progress despite all three of its children already `[x]`. Verified directly against code (not just re-reading DEV_LOG) before flipping anything: `schema.prisma` has both models, `backend/src/retraining/retraining.service.ts` has the `@Cron` trigger, `ReportsModule`/`ModelsModule` exist with the endpoints described above. Flipped `4.2`, `4.3.1`–`4.3.4` to `[x]` in `WBS.md`; updated `4.3.5`'s note to drop the now-stale "blocked on 4.3.1" framing (the table exists now — the real remaining gap is that `ai/retraining/reports.py` has no `DatabaseReportSource` class yet, and no real GPU fine-tune has run). Also verified the rest of Sprint 4 Track C (mobile) against the code rather than trusting the existing WBS notes: confirmed `MessageFilter` still has no `UNKNOWN` case (4.3.10), `AISummaryBottomSheet.kt` still hardcodes `"68%"/"92% confidence"` with no `SummarizeApi.kt` anywhere in `mobile/` (4.3.11), and `BlockedNumbersViewModel.kt` still has no network call (4.3.12) — all three genuinely still open, WBS notes were accurate. `4.4.3` (integration test: full retraining round trip) confirmed not started — no matching test file exists. Propagated `4.2`/`4.3.1`–`4.3.4` into `docs/development/BantAI_WBS.xlsx` (Task Status → Complete, Actual Completion Date 2026-08-12) and `docs/development/BantAI_Sprint_Schedule.xlsx` (Sprint Backlog rows Not started → Done for the same three Track A items; Sprint Summary as-of note updated; Gantt Overview's `Sprint 5` header flipped from `(Upcoming)` to `(In Progress)` since real Sprint 5 work — 5.3.2, 5.3.6 — is already well underway). Sprint 4 ceremony items (4.1.1, 4.5) confirmed with Gio as genuinely not done yet — left unchanged. **Follow-up same day:** Gio asked whether other members' Sprint 5 progress was also checked — it hadn't been beyond the Gantt header. `git fetch --all` confirmed no unmerged work on any remote branch (`feature/backend-sprint4-reports-models`, `feature/sprint4-retraining-pipeline`, `feature/sprint5-centering-verification`, `feature/sprint5-clustering-thresholds`, `develop-web-frontend`, `feature/debug-sms-testing-tools` are all fully merged into `develop`, zero commits ahead). But `5.3.6` (HDBSCAN threshold re-calibration) was found stale in both xlsx trackers — "Not Started" despite `WBS.md` already correctly tracking it as heavily in-progress (0.85→0.999 recalibration, lexical corroboration signal, embedding re-centering bracketed and not adopted, pending adviser sign-off). Flipped to "In Progress" in `BantAI_WBS.xlsx` and the Sprint Backlog sheet of `BantAI_Sprint_Schedule.xlsx`, with a short status summary added to the backlog row description. | Gio |
| 2026-08-16 | Backend | Two small endpoints added to unblock the remaining Sprint 4 mobile items — neither touches AI/ML's still-in-progress retraining work (WBS 4.3.5), they were just missing plumbing. (1) `POST /api/ai/summarize` (`AiController`, `AiService.summarize()`, JWT-guarded): proxies the AI service's already-existing `POST /summarize` (built 2026-08-04, WBS 4.3.9) — nothing in the backend called it until now. Unreachable/erroring AI service surfaces as a 503 (`ServiceUnavailableException`) since summarization has no on-device fallback the way `/classify` does. (2) New `BlockedNumbersModule` (`GET /api/blocked-numbers`, `POST /api/blocked-numbers`, `DELETE /api/blocked-numbers/:sender`, all JWT-guarded): the `BlockedNumber` Prisma table has existed since Sprint 3 (auto-populated by `/sms/ingest`'s auto-block step) but had no controller, so nothing could read it back or unblock through it. `block()` upserts idempotently (mirrors `sms.service.ts`'s own auto-block upsert — `update: {}` so re-blocking never clobbers an existing `AutoBlock` row's `source`); `unblock()` 404s on a row that's already gone rather than crashing on Prisma's `P2025`, matching the project's existing `NotFoundException` convention. Both reuse the same private `normalizePhone()` already duplicated in `sms.service.ts`/`verification.service.ts` rather than introducing a shared util as an unrelated refactor. 15 new tests (`ai.service.spec.ts`, `ai.controller.spec.ts`, `blocked-numbers.service.spec.ts`, `blocked-numbers.controller.spec.ts`); full suite 167/167 passing (up from 152). Not committed. | Gio |
| 2026-08-16 | Android | Closed all three remaining Sprint 4 Track C items (WBS 4.3.10–4.3.12), unblocked by the two backend endpoints above. **4.3.10 (Unknown Filter):** added `MessageFilter.UNKNOWN` to `MessagesViewModel.kt` and wired it into `MessagesScreen.kt`'s filter dropdown — filters on the `classification == "unknown"` bucket that was already being computed for the Home tab's badge count but had no dedicated filter tab. **4.3.11 (AI Summary):** new `SummarizeApi.kt` (same plain-`HttpURLConnection` pattern as `SmsApi.kt`, no Retrofit) calls the new `/ai/summarize` proxy from `MessageDetailScreen.kt` with the real thread's message bodies the moment the sheet opens, fetched once per thread and reset when the sender changes; `AISummaryBottomSheet.kt` now renders that real extractive summary with a loading state, and the fabricated `"68%/92% confidence"` line is gone — the verdict badge was already real (derived from stored classification), only the confidence number was invented, and there was no real per-message confidence figure persisted on-device to replace it with, so it was removed rather than re-faked. `SuspiciousDetailScreen.kt` (confirmed dead — registered in `NavGraph.kt` but nothing navigates to it) needed no changes since the two new bottom-sheet parameters default to null/false. **4.3.12 (Blocked Numbers sync):** new `BlockedNumbersApi.kt`; `BlockedNumbersViewModel.kt` now reconciles the device's `BlockedNumberContract` list against the backend on load — applies backend-known blocks the device doesn't have yet (e.g. fresh install), and pushes device-only blocks up (covers the offline local-heuristic classification path, which never calls the backend at all) — and `unblockNumber()` now calls both the system contract and the backend `DELETE`, best-effort on the network side so a sync failure never blocks the local action. `compileDebugKotlin` passes (`BUILD SUCCESSFUL`, via Android Studio's bundled JBR). Not committed. | Gio |
| 2026-08-16 | Docs | WBS/tracker sync: merged PR #46 (`feature/sprint4-mobile-wiring`, `bc30c48`) closes out Sprint 4 Track C — flipped `4.3.10`, `4.3.11`, `4.3.12` from `[-]` to `[x]` in `WBS.md`, `BantAI_WBS.xlsx`, and the Sprint Backlog sheet of `BantAI_Sprint_Schedule.xlsx`. `4.3` parent stays `[-]` — `4.3.5` (AI/ML's retraining pipeline report source) is still open. | Gio |
| 2026-08-16 | ML | Code quality standards, phase 1 of 4 (`ai/` — cheapest/lowest-risk stack, done first by design). `pyproject.toml`'s `[tool.ruff]` previously set `line-length`/`target-version` only, with no `[tool.ruff.lint] select` — meaning it was silently running whatever this machine's ambient ruff defaults were rather than a deliberately chosen rule set (348 findings surfaced the moment `select` was actually specified explicitly and machine-independently). Deliberately narrow first pass: `select = ["E", "W", "F", "I"]` (real correctness/import-hygiene issues) — bugbear (`B`) and pyupgrade (`UP`) both trialed and found 300+ pre-existing findings each, almost entirely cosmetic (old-style `Optional[X]` typing, `%`-string formatting), so left as a documented, separate follow-up rather than dumped into one commit. `line-length` bumped 88→120 (matches this codebase's long narrative-comment style); `colab/` notebook excluded (its `%cd`-before-imports pattern is a legitimate Colab convention, not a violation). Two real (if minor) findings fixed by hand rather than deferred: `training/dataset.py` had a function return-annotated `-> \"pd.DataFrame\"` with `pandas` imported only inside the function body, tripping `F821` on the quoted annotation — moved the already-hard-dependency `pandas` import to module level and dropped the now-unnecessary quotes; `service/campaign.py`'s campaign-matching loop tracked an unused `best_id` alongside `best_sim` (`F841`) — removed, `best_sim` was always the only one actually read. Five `for l in ...` single-letter loop variables renamed (`line`/`lab`/`lang`) across `retraining/snapshot.py` and four `scripts/*.py` analysis tools — pure readability, no logic touched. Added `ruff>=0.16` to `requirements.txt`. Verified: `ruff check .` clean (both the ambient global install and `ai/.venv`), full suite 322/322 passing (`ai/.venv`) — confirms none of the hand-edits changed behavior. Not committed. Next: `backend/` (tooling already exists, just needs CI), then `web/` (nothing exists yet), then `mobile/` (ktlint + detekt, biggest lift), then one CI workflow tying all four together. | Gio |
| 2026-08-16 | Backend | Code quality standards, phase 2 of 4 (`backend/` — tooling already existed, this was mostly cleanup + closing real gaps found along the way). Two genuine environment bugs surfaced and fixed before lint results were even trustworthy: (1) the Prisma client was stale — regenerating it (`npx prisma generate`) dropped ESLint's error count from 215 to 124 on its own, all false "unsafe access" noise from outdated generated types; (2) `@nestjs/schedule` was declared in `package.json`/`package-lock.json` but genuinely absent from `node_modules` (`npm ls` confirmed empty) — explains the remaining "type could not be resolved" errors on `@Cron`/`ScheduleModule.forRoot()`; `npm install` pulled in the 4 missing packages. Also ran `npm audit`: 5 high-severity advisories, all in transitive dev/test dependencies (`brace-expansion`, `fast-uri`, `js-yaml`, `multer`) — `npm audit fix` resolved all 5 with non-breaking updates, 0 vulnerabilities remaining. With the environment actually correct, real lint findings dropped to 21 (7 errors, 14 warnings) from an original 215. Fixed by hand rather than deferred: a genuinely-unnecessary regex escape in `sms.service.ts`'s email-masking pattern (`\-` at the end of a character class is already literal — `no-useless-escape`), an unawaited promise in `sms.controller.spec.ts` that happened to still pass by coincidence (assertion was synchronous, but a real bug waiting to happen), an unused import in `main.ts`, two test mocks with an unnecessary `async` keyword, and `jwt.strategy.ts`'s `validate()` — declared `async` but never awaited anything; Passport-JWT supports synchronous `validate` callbacks, verified safe via the full auth suite. Two spots left as scoped, commented `eslint-disable` rather than restructured: `ai.service.ts`'s `res.json()` (the Fetch API itself types this `Promise<any>` — unavoidable without adding runtime schema validation, out of scope here) and `auth.module.ts`'s JWT `expiresIn` cast (works around `jsonwebtoken`'s branded `StringValue` type). Added a `files: ['**/*.spec.ts', 'test/**/*.ts']` override in `eslint.config.mjs` disabling `no-unsafe-*`/`unbound-method` for test files specifically — both are documented typescript-eslint false-positive sources against `jest.fn()` mocks and `expect(x.method).toHaveBeenCalled()`, not real bugs. Left as warnings (not errors, non-blocking, lenient-tier deferral): 3 `no-unsafe-argument` and 10 `no-floating-promises` in `campaigns.*.spec.ts`, all test-file-only. Also added `"endOfLine": "auto"` to `.prettierrc` itself (previously only set as an inline ESLint override, so bare `npx prettier --check` disagreed with what `npm run lint` actually enforced — now both agree). Verified: `eslint` 0 errors/13 warnings, `prettier --check` fully clean, `nest build` clean, full suite still 167/167. Not committed. | Gio |
| 2026-08-16 | Dashboard | Code quality standards, phase 3 of 4 (`web/` — nothing existed here before this pass; first-ever lint/format run on the codebase). Built `eslint.config.js` from scratch by combining backend's typescript-eslint + prettier setup with truepositive's React rules (neither alone fit — bantAI's web is TypeScript+React, truepositive's frontend is plain JS). Two real, non-cosmetic bugs found and fixed before lint results were trustworthy: (1) `vite.config.js` + `vite.config.d.ts` — stray `tsc`-compiled output of `vite.config.ts` — were accidentally committed to git alongside the real source; beyond breaking type-aware linting (neither file is covered by `tsconfig.node.json`'s `include`), having both tracked risked the stale `.js` silently shadowing future edits to the `.ts` source — removed from git and added to `.gitignore`. (2) `npm audit`: 4 vulnerabilities (1 moderate, 3 high — `nanoid`, `postcss`, `react-router`/`react-router-dom`), all resolved non-breaking via `npm audit fix`, 0 remaining. First full lint run: 3230 problems, 3089 auto-fixable (the codebase had never been formatted — single vs double quotes, missing commas, throughout). After `--fix`: 141 real findings. Fixed by hand: 15 genuinely-unused imports/variables (including a fully-dead `[totpCode, setTotpCode]` state pair and an entire unused mock-data import block in `admin.tsx`), 6 unescaped-quote JSX text nodes in small pages, an unsafe `location.state` access in `TwoFactorPage` (added a proper `TwoFactorLocationState` interface instead of suppressing), two `JSON.parse()` calls in `UserAvatarContext.tsx` missing a type assertion, and one `onClick` handler tripping `no-misused-promises` on `navigate()`'s inferred return type (wrapped in a block body). Deferred, documented in the config rather than mass-edited: `no-unsafe-{assignment,member-access,call}` and `no-misused-promises` in `admin.tsx`/`client.tsx`/`public.tsx` specifically — traces to one root cause (`src/mocks/referenceData.ts` has no TypeScript interfaces, so anything destructured from it is `any`) that's a real, worthwhile follow-up but not a blind per-line edit across three files totaling 15,000+ lines I don't own. `react/no-unescaped-entities` and `no-floating-promises`/`no-unsafe-argument` (kept as warnings, not errors) left as the same lenient-tier deferral. Verified: `eslint` 0 errors/21 warnings, `prettier --check` fully clean, `tsc -b && vite build` succeeds, dev server serves 200 with a working page. Not committed. | Gio |
| 2026-08-16 | Android | Code quality standards, phase 4 of 4 (`mobile/` — the last stack, nothing existed here before this pass). Added ktlint (`org.jlleitschuh.gradle.ktlint` 12.1.1, ktlint engine 1.5.0) for formatting and detekt (`io.gitlab.arturbosch.detekt` 1.23.7) for static analysis — the Kotlin-ecosystem equivalent of Prettier+ESLint, mirroring the pattern already applied to `ai/`, `backend/`, and `web/`. First `ktlintFormat` run flagged hundreds of "function name should be lowercase" errors — almost all on `@Composable` functions, which are conventionally PascalCase (`MessageDetailScreen()`, used like a widget) and not a real violation; ktlint doesn't know this convention by default. Fixed properly via `.editorconfig` (`ktlint_function_naming_ignore_when_annotated_with = Composable`) rather than baselined, since it's a permanent correct exception that would otherwise keep firing on every new screen. That one config line cleared the overwhelming majority of findings. What was left (27, across the whole app): 9 wildcard imports (`androidx.compose.material3.*` etc. — expanding a few UI files to dozens of explicit names by hand is pure churn, disabled via `.editorconfig` with a documented reason), ~14 over-length lines that are long educational copy-text string literals in `TipDetailScreen.kt`/`PrivacyDataScreen.kt` (ktlint's own line-length check turned off in favor of detekt's, so it's enforced once, not twice, by two tools with two different limits), and 4 genuine `backing-property-naming` violations in `MessagesViewModel.kt` — `_allMessages`/`_recentlyDeleted`/`_draftRows`/`_scanPeriod` used the `_foo`/`foo` backing-property convention without an actual public `foo` counterpart existing (they're purely internal state), so fixed for real by dropping the misleading leading underscore (renamed, all call sites updated, verified no naming collisions) rather than suppressed. detekt: `buildUponDefaultConfig = true` plus a baseline (`app/detekt-baseline.xml`, 441 pre-existing findings grandfathered in — expected for a first-ever static-analysis pass across ~80 files, mostly `LongMethod`/`ComplexMethod` on Compose UI functions) so only genuinely new issues in future PRs fail the build, same "start lenient" approach as the other three stacks. `app/detekt.yml` sets `MaxLineLength` to 120, matching the width already chosen for `ai/`, `backend/`, and `web/`. No dependency-vulnerability scan added here (unlike `npm audit` for the other three stacks) — Gradle has no zero-config equivalent; adding OWASP dependency-check would mean introducing and configuring a new, heavier plugin, left as a separate follow-up rather than bolted on here. Verified: `ktlintFormat` and `detekt` both clean, `compileDebugKotlin` → `BUILD SUCCESSFUL` (only pre-existing, unrelated deprecation warnings). No unit test suite exists under `mobile/` to run (confirmed, not a regression — this project has never had one). Not committed. | Gio |
| 2026-08-16 | Docs | Closed two gaps flagged after the code-quality rollout: docs never mentioned the new tooling, and `ai/` was the only stack of four without a dependency-vulnerability scan. (1) Added `pip-audit` to `ai/requirements.txt`; ran it — 0 known vulnerabilities. `backend/`/`web/` already covered via `npm audit`; `mobile/` documented as a deliberate gap (Gradle has no zero-config equivalent) rather than silently skipped. (2) Added a "Code Quality & Security" section to `backend/README.md`, `ai/README.md` (also fixed a stale roadmap line there — it still said retraining was "blocked on Track A's UserReports table," which landed 2026-08-10), and the root `README.md` (summary table across all four stacks). Wrote `web/README.md` and `mobile/README.md` from scratch — neither existed before. (3) Added an "Ownership & Responsibility" section to `CLAUDE.md` — who owns `docs/`/WBS/merges (Gio), who owns each folder (Track A-D per the existing WBS mapping), and how cross-track edits should be logged when someone unblocks another track's work (as happened this session with the two backend endpoints for mobile). Flagged as a first draft in the doc itself, not presented as final. Corrected the Team table's stale claim that Reymark owns "git management" — confirmed with Gio that he's the actual merge gatekeeper. Not committed. | Gio |
| 2026-08-16 | Docs | Re-verified all four stacks clean before building the CI workflow — found one real regression in the process. `web/`'s `vite.config.js`/`vite.config.d.ts` (removed earlier this session, WBS-adjacent code-quality pass) were back and crashing ESLint again — turned out `npm run build`'s `tsc -b` step regenerates them every single run, because `tsconfig.node.json` has `composite: true` with no `outDir`, so it emits compiled JS alongside the `.ts` source by default. The earlier fix (delete + `.gitignore`) only addressed the symptom (accidental commits) — the actual generator was never turned off, so it would have silently broken the first CI run the moment a build step ran before lint. Real fix: added `"outDir": "./node_modules/.tmp/tsconfig.node"` to `tsconfig.node.json`, redirecting the required composite-project emit to an already-gitignored location instead of the project root. `composite: true` doesn't accept `noEmit: true` (TypeScript errors: "Composite projects may not disable emit"), so `outDir` is the correct fix, not a workaround. Verified: clean `npm run build` no longer creates anything at `web/` root, `eslint .` clean afterward (0 errors, 21 warnings, matching the known baseline). Also reconfirmed `ai/` (ruff clean, 322/322 tests), `backend/` (eslint 0 errors, 167/167 tests, clean build), and `mobile/` (`ktlintCheck`, `detekt`, `compileDebugKotlin` all `BUILD SUCCESSFUL`) — all still exactly as left. Not committed. | Gio |
| 2026-08-16 | Docs | Added `.github/workflows/ci.yml` — bantAI had zero CI despite truepositive (the reference project) having one; nothing was enforcing any of the lint/test/security work above automatically. Four jobs, one per stack, modeled on truepositive's `ci.yml` structure: `ai` (ruff check + format check, pytest, pip-audit, Python 3.11), `backend` (prisma generate → eslint → prettier check → jest → nest build → npm audit, Node 22), `web` (eslint → format check → build → npm audit, Node 22), `mobile` (ktlintCheck + detekt + compileDebugKotlin, JDK 17 + Android SDK via `android-actions/setup-android`). Triggers on push/PR to `main` and `develop`. Before writing it, checked whether any test/build step secretly depends on an env var or secret that only exists in local `.env` files (which are gitignored and won't exist on a CI runner) — confirmed clean: no `*.spec.ts` file imports `constants.ts` or `AuthModule` (the only code path that throws on a missing `JWT_SECRET`), `prisma generate`/`nest build` never execute that code either (compile-only), and every field in `ai/service/config.py`'s `Settings` class has a default (instantiating it with zero env vars set never throws) — so neither the `backend` nor `ai` job needs any GitHub Secrets configured. `npm audit` steps use `--audit-level=high` so a newly-introduced vulnerability fails the build going forward, not just today's already-clean baseline. Caveat, stated plainly rather than overclaimed: every command in the workflow was verified locally (that's the whole point of the same-day re-verification pass above), but the workflow YAML itself has not run on GitHub's actual runners yet — first real confirmation happens on the first push. Not committed. | Gio |
| 2026-08-16 | Docs | First real CI run (on push, before merge) failed in 3 of 4 jobs — the caveat above turned out to matter. All three traced back to me testing a narrower local scope than what's actually in `ci.yml`. **backend** (`npx prettier --check .`): I'd only ever run `prettier --check` against `src/**/*.ts` locally; the bare `.` CI uses also covers `database/`, root config, and docs. Turned up a real, separate bug along the way: `backend/database/prisma/generated/` (8 files, dated Jul 15) was a **stale, tracked-in-git copy of the Prisma client** — `schema.prisma` has no custom `output`, so current `prisma generate` runs write to `node_modules/@prisma/client` instead, meaning this directory was dead weight nobody was regenerating; confirmed nothing in `src/` imports from it, removed it from git, gitignored it, added `backend/.prettierignore` as a backstop. Formatted the 3 genuinely-real files left after that (`README.md`, `eslint.config.mjs`, `database/prisma.module.ts`). **ai** (`ruff format --check .`): I'd only ever run `ruff check` (lint) locally, never `ruff format` (the formatter) — two different commands, and I'd only tested the first. Ran `ruff format .`, 58 files reformatted (whitespace/style only), reverified 322/322 tests still pass. **mobile** (`Could not find or load main class org.gradle.wrapper.GradleWrapperMain`): investigated thoroughly before touching anything — compared the committed git blob against the working-tree `gradle-wrapper.jar` byte-for-byte (identical, valid `PK` zip header, not corrupted) and checked `gradlew`'s committed line endings (clean LF, not CRLF) — so the two most common causes of this exact error didn't apply here. Found one real, verifiable issue: `gradlew` was committed with mode `100644` (not executable) — fixed via `git update-index --chmod=+x`. Could not fully reproduce the exact failure locally without live CI access, so applied defense-in-depth on top of that fix rather than guessing further: added `.gitattributes` (`gradlew text eol=lf`, `*.jar binary`) to guarantee no future Windows-checkout corruption regardless of cause, regenerated the wrapper fresh via `gradlew wrapper --gradle-version 8.9` to rule out any doubt about the jar's integrity, and changed the CI step from `./gradlew` to `bash gradlew` (removes any dependency on the execute bit or shebang resolving correctly on the runner). Re-verified `bash gradlew :app:ktlintCheck` locally after the change — still works. Re-verified all four stacks end-to-end against the exact commands in `ci.yml` (not narrower local approximations this time): `ai` ruff check + format check + 322/322 tests, `backend` lint + prettier + 167/167 tests + build, `mobile` bash-invoked ktlintCheck. All clean. Not committed — next push is the real test of the mobile fix specifically, since that one couldn't be confirmed locally. | Gio |
| 2026-08-17 | ML | Closed WBS 4.3.5 completely — both remaining gaps. **(1) `DatabaseReportSource`** now reads Track A's `UserReport` table (`GET /api/reports`, ApiKeyGuard), keeping `status == "Validated"` and mapping `message.body`→text, `reportedLabel`→label, `id`→report_id, `updatedAt`→validated_at. It drops in behind the existing `fetch`/`describe` interface with **zero** change to `snapshot.py` or `pipeline.py` — the seam working as designed. `reportedLabel` not `originalLabel`: the original is the mistake being corrected, and training on it would teach the model to repeat what was reported. Three decisions where the obvious implementation is wrong: the **Validated filter runs client-side** (`ReportsService` exposes only `findAll()`/`findPending()`; fine at thesis scale, and the fix if the table outgrows one unpaginated response is a server-side filter on Track A's side, not paging against an API with no cursor); **`validated_at` is really `@updatedAt`**, which moves on any write including an `adminNote` edit — a proxy, exact only with a dedicated `validatedAt` column; and **failures raise instead of degrading**, deliberately the opposite of `centroid_source.py`, because an empty centroid list costs one enhancement whereas an empty *report* list is indistinguishable from "no corrections were filed" and would yield a clean-looking retrain that learned from none of the mistakes that triggered it. Verified against a live backend rather than stubs: seeded one Validated + one Pending + one Rejected report, got `1 of 3` with the snapshot's Scam count up by exactly one (proving the field mapping, not just the count), and confirmed a wrong key raises a 401 instead of reporting zero. **(2) First real GPU fine-tune**, Colab T4, 2026-08-17 — the pipeline had only ever been dry-run. Full path executed: snapshot → fine-tune → score both checkpoints on the same rows → gate. Result **promote**: macro-F1 0.9520 → 0.9648, 97 fixes vs 44 regressions, p=0.0000095, `skipped_reason: null`. Evidence in `ai/evaluation/retraining_run_2026-08-17.json` (not under `ai/models/`, which is git-ignored — the weights stay on Drive and are regenerable from the manifest). **The comparison was conservative, and that is now observed rather than assumed:** `AI_MODEL_RESULTS.md` records the deployed model at macro-F1 0.9438 on its own split, but it scored 0.9520 here — the dataset grew after 2026-07-30, so the stratified split partitions differently and the baseline was partly graded on rows it had memorised. Bias favours the incumbent, so the true gap is likely wider than +1.3pp. The gain comes from the 1,050 + 447 new messages folded in on 2026-07-30, which the baseline never saw; no relabelling happened in that window, so it is not a labels-changed artefact. Run with **zero reports on purpose** — the only validated reports that ever existed were a smoke-test fixture with invented bodies, deleted first, because training a real checkpoint on fabricated SMS would contaminate every figure derived from it. **Found and fixed a live bug next door:** `GET /campaigns/centroids` is ApiKeyGuard-protected but `centroid_source.py:load_from_backend` sent no `x-api-key` at all, and since `load_centroids` swallows failures by design the default `centroid_source=backend` was 401ing silently into "0 campaigns loaded" — campaign matching has been off in the backend configuration and nothing said so. Compounding it, `INTERNAL_API_KEY` is absent from the backend env setup entirely, so the guard also throws "not configured on the server" on a fresh clone. Also fixed `pipeline.py:_predict` never calling `.to(device)` (the gate scored both checkpoints on CPU even on a T4). Suite 322 → 359 tests; ruff, ruff format and pip-audit all clean; all four CI jobs green. **Nothing was promoted** — campaign centroids are tied to the checkpoint that made them and a swap would invalidate every Sprint 5 threshold number currently with the adviser (see 5.3.6); this is now a fifth item awaiting sign-off there. **Follow-up the same day:** asking "what did the new model break?" surfaced that the gate counted regressions but never recorded which rows — `decision.json` now carries a text-free class-level breakdown (`"regression_transitions": {"Scam->Spam": 20}`) safe to commit and quote; row-level detail with message text goes to git-ignored `disagreements.json` only. Building that surfaced a real bug: `PromotionDecision.__bool__` returns `promote`, so a *rejected* decision is falsy, and two call sites tested it for existence rather than `is not None` — `decision.json` was writing `"decision": null` for every rejected candidate, discarding the reason, both F1 scores and the p-value, and the `REJECT` branch of `summary()` was unreachable. Fixed with `is not None`, regression-tested; suite now 373. Merged to `develop` via PR #52 (`75310ad`), 2026-08-18. | Maxene |
| 2026-08-18 | ML | Closed WBS 4.4.3 — full retraining round trip, built and verified live against real Docker Postgres + backend + AI service (not stubs). Two real gaps closed: **(1)** the backend's hourly cron already POSTed to `${AI_SERVICE_URL}/retrain`, but the AI service had no such route — the 404 was silently swallowed as a warning (`retraining.service.ts:148`). Now it does: `POST /retrain` records the request and queues it (no GPU on the serving host, so it queues rather than trains), `GET /retrain/jobs` reads the queue back. **(2)** `ModelVersions` stayed permanently empty — `pipeline.py` scored a candidate and wrote `decision.json`, then stopped, never telling the backend a candidate existed, so the backend's own F1-degradation trigger and rollback route were dead code (`activeModel` always `null`). `scripts/retrain.py --register`/`--activate` now do that, and `version.json` travels with a checkpoint so `GET /health` can report which version is actually serving. Nothing changes what's deployed by itself: `--activate` refuses a rejected candidate, and activating a `ModelVersion` row still isn't a redeploy — pointing the live model at `<candidate_dir>` stays a manual step, on purpose. **Verified live 2026-08-18** via `scripts/round_trip.py` against real infra (Docker Postgres, `npm run start:dev`, `uvicorn service.main:app`), all five stages passed: seeded one Validated/Pending/Rejected report directly in Postgres (no dev-mode way to read a generated OTP to seed via the normal app flow); `DatabaseReportSource` read the live table and kept exactly the Validated one, wrong API key failed loudly with a 401; `POST /retrain` returned 202 and queued a real job, a repeat trigger deduped to the same job instead of a second row; the promotion gate ran for real (a real forward pass, not training) and produced a real decision; registered a `vROUNDTRIP-TEST-...` `ModelVersion`, confirmed it was never activated, cleaned up afterward. Found and fixed one real bug during the live run: the script's own scratch directory didn't exist yet when the gate tried to write `disagreements.json` into it — fixed, second run was clean. Also gave `ModelVersions` its first real row the same session (`scripts/register_incumbent.py --activate`): the currently deployed 2026-07-29 checkpoint, macro-F1 0.9438, now the active `ModelVersion` — confirmed via `GET /health` reporting `version_tag: "v2026-07-29-run3"` and `GET /api/models/active` matching, meaning the backend's F1-degradation trigger and rollback route are live code now, not dead code. Suite: 373 → 397 (24 new — 21 round-trip + 3 indicator-tag), all green; `ruff check`/`ruff format --check` clean. **Unblocks 4.5.1** (Sprint 4 demo — "retraining trigger fires end-to-end" is now literally true and demoable). Merged to `develop` via PR #53 (`72b78a8`), 2026-08-18. | Maxene |
| 2026-08-18 | ML | Independently re-verified WBS 4.4.3/4.5.1 rather than closing on Maxene's write-up alone: full local suite (397/397), `ruff check`/`ruff format --check` clean, then `ai/scripts/round_trip.py` re-run end to end against real Docker Postgres, a real `npm run start:dev` backend, and a real AI service on :8001. All 5 stages passed exactly as claimed. Two environment-only gaps found and fixed, neither a code bug: this machine's local Postgres was missing the Sprint 4 migration entirely (`UserReport`/`ModelVersion` tables didn't exist — `npx prisma migrate deploy --schema=database/prisma/schema.prisma` applied it), and `round_trip.py`'s default `--api-key` (`dev-internal-key-4335`) doesn't match this machine's actual `backend/.env` `INTERNAL_API_KEY` — passed explicitly to get past the 401s. Cleaned up the resulting `vROUNDTRIP-TEST-...` `ModelVersion` row afterward, same as the original run. Closes WBS 4.5.1 (Sprint 4 demo) on this verification. | Gio |
| 2026-08-18 | All | Sprint 4 retrospective held, closing WBS 4.5.2 and Sprint 4 overall. **Went well:** Build (4.3), Test (4.4), and Demo (4.5.1) all closed this sprint with independent re-verification rather than one contributor's word taken at face value — the retraining round trip was re-run live against real infra by a second person and matched every claim exactly. **Known limitation, carried forward rather than blocking Sprint 4 close:** the mobile app has no deployed backend yet. Every screen backed by a live API call — Alerts and Campaigns are the two the team actually dogfoods day to day, so that's where it's felt most — only works while the phone is tethered to whichever laptop is running the backend, via `adb reverse tcp:3000 tcp:3000` on USB or the `10.0.2.2` loopback on an emulator (`mobile/app/src/main/java/com/bantai/data/remote/ApiConfig.kt`). No on-device model, and no committed timeline yet for one — classification and retraining both stay laptop-hosted for now. Not treated as a gap in Sprint 4's own scope: real deployment is explicitly Sprint 6 work ("Distribute app via APK to test group and administrators," Phase 1: Limited Deployment per the Gantt), so it's flagged here for visibility going into Sprint 5/6 rather than reopening Sprint 4. Also closed 4.1.1 retroactively (never run as its own analysis pass, but its scope was fully absorbed by 4.2.1's state machine and 4.3.1's endpoints, both shipped and exercised live with no requirements gap surfacing) — 4.1, 4.5, and Sprint 4 (top-level `4`) all now `[x]`. | Gio |
| 2026-08-18 | ML | Sprint 5 groundwork, closing WBS 5.2.2 and 5.3.7 — found stale while auditing Sprints 1–5 for tracker accuracy (both had already merged via PR #54 but `WBS.md` was never flipped). **5.2.2** (`ai/REFINEMENT_PLAN.md`, new): documents that S2–S4 collected data is the 1,050 + 447 real phone-inbox messages added 2026-07-30, and that refinement reuses Sprint 4's retraining pipeline unchanged rather than a second training implementation — which already ran for real on 2026-08-17 (4.3.5) with a promote verdict, so 5.3.5's execution is effectively done and what remains is the promotion decision already gated under 5.3.6. **5.3.7** (SHAP indicator tag dictionary polish): `scripts/analyze_indicator_coverage.py` (new) measured the keyword tagger against every labeled Spam/Scam row rather than re-reviewing by eye — Spam's 84.9% zero-tag rate is expected (ordinary marketing, correctly untagged), Scam's 48.6% was the real gap, traced to a second wave of Tagalog online-betting vocabulary (W+, T1BET, Epicwin, Betso88, COD63/COD99 schemes) and a phishing-phrasing gap ("...to avoid Deactivation"). Both added to `indicator_tags.py` with a Ham false-positive check (<0.1%); Scam zero-tag rate 48.6% → 40.0%, deliberately not chased further (remaining tail is one-off wordings). `test_indicator_tags.py` 17 → 20 tests. Merged to `develop` via PR #54 (`5b1ca27`), 2026-08-18. | Maxene |
| 2026-08-18 | Docs | Audited Sprints 1–5 for tracker accuracy across `WBS.md`, `DEV_LOG.md`, and both xlsx trackers, prompted by closing out Sprint 4. Sprints 1–3: all 84 WBS.md line items cross-checked 1:1 against `BantAI_WBS.xlsx` (all `Complete`) and the Sprint Backlog sheet of `BantAI_Sprint_Schedule.xlsx` (all `Done`) — no mismatches found. Found and fixed one real formatting bug in this file: a stray blank line between the 2026-08-11 and 2026-08-10 Progress Log rows was silently splitting the Markdown table in two, meaning everything from 2026-08-10 onward would render as broken/plain text (not a table) on GitHub or any Markdown viewer — removed. Found two real staleness gaps in Sprint 5: `5.2.2` and `5.3.7` already merged via PR #54 but never flipped in `WBS.md` — closed above. **Follow-up, same pass:** ran a full programmatic cross-check of every WBS item's status between `WBS.md` and `BantAI_WBS.xlsx` (171 items each side) rather than spot-checking — found 5 more mismatches, all `WBS.md` correct and the xlsx stale, verified against actual code before fixing rather than trusting either tracker blindly: **4.3.8** (campaign evolution tracking) — `ai/campaign_evolution.py` (386 lines) + `scripts/track_campaign_evolution.py` (CLI) + 30 tests all exist and are wired together; xlsx said "In Progress." **4.4.1** (report intake → dataset append unit test) — covered across both tracks (`reports.service.spec.ts` 18 tests + `ai/tests/test_reports.py` 31 tests); xlsx said "Not Started." Fixed in both `BantAI_WBS.xlsx` and the Sprint Backlog sheet of `BantAI_Sprint_Schedule.xlsx`. **4.2.1** (report feedback state machine) — real nuance surfaced, not just a stale-tracker fix: the task's own title names four states (Pending/Validated/Rejected/**Resolved**), but the shipped code (`schema.prisma` line 181, `reports.controller.ts`) only ever built three — no `Resolved` status or endpoint exists anywhere. Left `[x]` since the 3-state version is what actually shipped and has been exercised live end to end, but flagged explicitly in both `WBS.md` and the xlsx rather than silently letting the task title overstate what was built. | Gio |
| 2026-08-18 | Android | Closed WBS 5.3.9 (mobile UI polish based on dogfooding) — ran the real onboarding-through-main-app flow live on a Pixel_6 emulator, not just a code review, and fixed what it surfaced. **Real bugs found and fixed:** (1) `OnboardingTermsScreen`/`OnboardingAllowAccessScreen` were fully built screens never registered in `NavGraph.kt` (a `// Stubs retained so orphaned screen files compile` comment confirmed it) — no user ever saw a Terms/Privacy consent screen despite the app requesting broad SMS/Camera/Contacts/Mic/Phone access (disclosed by Android's own default-SMS-app dialog); now wired into the flow between Default-SMS-app→Confirm-number and Enter-code→Profile respectively. (2) Phone numbers were being stored without the `+63` country code — `OnboardingViewModel.requestOtp` only stripped spaces — confirmed directly against the DB (manually-typed `9171234567` next to a correctly-formatted `+639458327611` row); added a real normalizer (`+63`/`63`/`0`-prefix aware). (3) "Send verification code" rendered identically whether the phone field was empty or not, and silently no-op'd on tap instead of giving any feedback — now visibly disabled via `enabled = !state.isLoading && phoneNumber.isNotBlank()`. (4) The bottom-nav notification badge was hardcoded and inconsistent between its two render states — confirmed in `MainScreen.kt`: the expanded bar always badged the Alerts icon regardless of actual alert count, while the collapsed pill badged every tab *except* Alerts, so switching to Messages/Campaigns/Settings always showed a red dot on a completely empty screen. Fixed by hoisting a single `AlertsViewModel` at `MainScreen` level (shared with the Alerts tab itself, avoiding a duplicate network call) and gating both badge sites on its real `alerts.isNotEmpty()`. (5) Profile screen's back arrow was a dead `onClick = {}`; now calls `navController.popBackStack()`. (6) Avatar initials lagged a recomposition behind while typing, because `viewModel.getInitials()` read internal fields Compose couldn't observe from inside a nested `AnimatedVisibility` scope — recomputed from the already-collected `firstName`/`lastName` State instead. **UI polish:** redesigned "Set up your profile" — bigger centered avatar (80dp→96dp) as the visual anchor, staggered cascade entrance animation (header→avatar→fields→button, not one uniform block), spring-bounce + color cross-fade on avatar tap, tightened copy/spacing. **Animation:** slide+fade transitions between every screen app-wide (`NavHost` enter/exit/popEnter/popExit), cross-fade between bottom-nav tabs instead of an abrupt cut. Every fix verified live after applying it (re-cleared app data, re-ran the flow, re-checked the DB/screenshots), not just rebuilt and assumed. Decomposed the new/touched composables to keep `ktlintFormat` + `detekt` + `assembleDebug` clean with zero new findings — one real detekt catch along the way (`LongMethod`/`CyclomaticComplexity` on the redesigned profile screen) fixed by extracting `ProfileAvatar`/`ProfileNameFields`/`ProfileActions` rather than suppressed; one legitimate `detekt.yml` addition (`FunctionNaming.ignoreAnnotated: ['Composable']`, matching the file's own stated "tune further once new code surfaces real findings" plan) since PascalCase is the actual Jetpack Compose convention every composable in this codebase already follows. Also caught and fixed a typo of my own mid-pass (a dropped `+` operator in a transition expression that ktlint reformatted around instead of erroring on). Not committed. | Gio |
| 2026-08-20 | Android | Mobile bug bash (WBS 5.3.8) + cross-device compatibility scoping (5.4.3), both left `[-]`. Two Explore passes audited the full mobile source tree; every finding independently re-confirmed against current code before fixing. Fixed a real crash (`SmsReceiver.kt`: a non-null-but-empty message array from a failed multipart PDU threw `NoSuchElementException` before the existing try/catch), an empty-sender data bug (`normalizeAddress`/`getInitialsFromSender` both only null-checked, not blank-checked), a negative-clock-skew display bug (`getRelativeTime`), missing error handling + a load race in `HomeViewModel`/`MessagesViewModel` (`loadMessages()` fired from 4 uncoordinated triggers with no cancellation or error state — added a cancelled-and-relaunched `Job` + `errorMessage` StateFlow matching the existing `AlertsViewModel` pattern), a phone-normalization bug in `OnboardingViewModel` (`0063`-prefixed numbers mangled, landlines/non-PH numbers silently forced into a fake `+63` value — rewritten to validate and reject instead), a silent DataStore-corruption reset in `UserPreferences` (now logged), an IME-clipping layout bug found in 3 onboarding screens (non-scrolling fixed-height `Column` — `OnboardingProfileScreen`, `OnboardingConfirmNumberScreen`, `OnboardingEnterCodeScreen`, the only 3 with real text input), and a missing-`Locale` date-formatting inconsistency across 6 screens (pinned to `Locale.US`, not device-default, since the app has no localized strings yet). Deliberately not fixed this pass, with reasons recorded in the WBS note: full i18n extraction, JWT plaintext storage (belongs to 5.2.1), `WapPushReceiver`'s no-op MMS handling (made non-silent instead of implemented), and the total absence of a test suite. `ktlintCheck`/`detekt`/`compileDebugKotlin`/`assembleDebug` all pass clean; detekt baseline regenerated via `:app:detektBaseline` (added 2 new legitimate entries, and dropped ~100 stale `FunctionNaming`/`LongMethod` lines a since-added `detekt.yml` Composable exemption had already made dead — confirmed via diff before accepting, not assumed safe). For 5.4.3, statically re-verified all 4 existing `SDK_INT`-gated branches by reading the actual code (not just the version guard), all correct. Live verification, both of the 5.3.8 fixes and of on-device Android 11+ behavior, was attempted but blocked by this session's sandboxed environment: the `Pixel_6` (API 33) AVD booted and the app installed and ran without crashing, but `com.android.systemui` repeatedly ANR'd (reproduced with both hardware and `swiftshader_indirect` software rendering, `system_server` pegged at 67-87% CPU), and direct SMS-injection attempts via both `adb emu sms send` and a raw emulator-console session returned `OK` but never landed a row in `content://sms/inbox` — consistent with degraded nested-virtualization/telephony emulation in this environment, not an app defect. Recovered partial live coverage on Gio's physical Huawei VOG-L29 (Android 10/API 29, real daily-use install, bantAI already the default SMS app): installed the fix build over the existing data, drove it via `adb` (`uiautomator dump` for exact tap targets, since the floating nav bar made blind coordinates unreliable) while tailing filtered logcat — zero crashes/exceptions across Messages/Settings/Compose navigation with real backend-connected data. Directly confirmed the Compose recipient-validation fix live: an invalid short recipient ("123") was rejected and verifiably nothing was sent (`content://sms/outbox`/`sent` both checked). Not exercised even on the physical device, and why: the `SmsReceiver` crash-guard (no way to inject a malformed SMS on real hardware via adb) and the onboarding phone-validation/IME-scroll fixes (both onboarding-only, would require clearing this device's real app data and re-triggering its default-SMS-app role — not done without asking first). This device is also API 29, below the Android-11+ scope of 5.4.3. Not committed; recommends either a working emulator or an explicit go-ahead to reset onboarding on a real device to close the remaining gaps before either item goes to `[x]`. | Gio |
| 2026-08-20 | Android | Live bug report from Gio (app going blank after pressing a top screen corner) reproduced and root-caused on his physical device, not guessed: `uiautomator dump` showed the window's content view had zero children while the process stayed alive with no crash logged, and logcat showed Huawei's `HiTouch_PressGestureDetector` firing immediately before `ViewRootImpl: dispatchDetachedFromWindow in doDie`. Cause: EMUI's floating-window gesture reparents the window without a normal Android recreate, and `MainActivity` in `AndroidManifest.xml` declared neither `resizeableActivity` nor `configChanges`, so the OS treated it as fully resizable. Fixed with `android:resizeableActivity="false"` + `android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"`. Recovered the stuck session live via `am force-stop` + relaunch. Checked every other ViewModel touching the SMS ContentProvider for the same missing-error-handling bug class and found two more real instances the original 5.3.8 audit had missed: `MessageDetailViewModel.loadConversation()` had no try/catch/error-state and the same overlapping-load race as the already-fixed `HomeViewModel`/`MessagesViewModel` — fixed identically, wired into `MessageDetailScreen.kt`; and that same screen's `retryFailedMessage()` was the only one of its three send-paths with no try/catch (could crash on a failed-message retry) — wrapped to match its siblings. Rebuilt clean; detekt baseline regenerated again (3 legitimate new entries total this session). Reinstalled on the physical device — launches and navigates normally. The corner-gesture fix itself still needs Gio to retry the actual trigger to confirm live; not committed. | Gio |
| 2026-08-20 | Android | Closed WBS 5.3.8 and 5.4.3. Gio committed (`8e1567d`) and merged to `develop` via PR #59, then confirmed all remaining live-verification items: the corner-gesture fix (retried the actual EMUI trigger — no longer goes blank), the burst-SMS load-race fix, the retry-after-failed-send fix, and — with an explicit go-ahead to reset onboarding on his daily phone — the phone-number rejection and profile-screen IME-scroll fixes. Only the `SmsReceiver` empty-PDU crash-guard stays code-review-only (no way to inject a malformed SMS on real hardware); accepted, since it's a narrow defensive guard rather than a behavior change anyone would notice failing. Re-ran a clean `ktlintCheck`/`detekt`/`compileDebugKotlin` against the merged `develop` HEAD post-merge to confirm nothing regressed — matches CI exactly, passed. 5.4.3 closes with an honest caveat recorded in the WBS: verification happened on Android 10/API 29, one version below the "Android 11+" the task names, since no working Android 11+ emulator or device was available — accepted given the declared `minSdk=26`/`targetSdk=35` range, the statically-confirmed `SDK_INT` branches, and that real device testing caught a genuine bug (the corner-gesture crash) an API-30+ emulator likely wouldn't have. | Gio |
| 2026-08-20 | ML | Maxene merged `feature/ai-holdout-test-set` to `develop` (PR #56 then #58, same branch iterated twice). Two pieces toward WBS 6.4.6 (Sprint 6, done as early prep — both commits say so explicitly, this is not the final deliverable): `scripts/create_holdout_set.py` carved a permanent 20% held-out split (`ai/datasets/holdout/`, seed `20260618`, 3,236 rows grouped so near-duplicates can't leak across the split — 1,785 Ham / 953 Spam / 498 Scam), backing up the original pool first; `scripts/evaluate_holdout.py` then scored the currently-deployed checkpoint against it (macro-F1 0.95, Spam the weakest class at F1 0.925 with 113 false negatives). That result is explicitly caveated in the holdout manifest itself: both the deployed 2026-07-29 checkpoint and the 2026-08-17 retraining candidate were trained on the full pool *before* this split existed, so neither can be honestly scored against it as a clean never-seen test — the real 6.4.6 result has to come from the first model trained after 2026-08-18, which hasn't happened yet. Same branch also shipped three unrelated fixes with their own value: a real typosquat-detection bug in `indicator_tags.py` (`host.lower().lstrip("www.")` strips any leading `w`/`.` characters, not just the literal prefix — e.g. mangles `www.wellsfargo.com` into `ellsfargo.com`, corrupting the domain check; fixed with an explicit prefix check), a TOCTOU race in `retrain_queue.py` (concurrent `/retrain` calls could both pass the dedup check before either wrote its job, producing a duplicate — fixed with a small cross-platform advisory file lock, `_FileLock`, since FastAPI runs sync routes in a thread pool so this was a same-process risk, not just a multi-instance one), and two `CampaignMatcher`/`lexical.py` performance fixes (tokenize a message once per match instead of once per centroid; stop recomputing per-centroid work that never changes). Verified independently, not taken on the PR's word: `ruff check`/`ruff format --check` clean, `pytest` 424/424 passed, `pip-audit` found no known vulnerabilities — all against the actual merged `develop` HEAD. WBS 6.4.6 marked `[-]` (prep only, not the real deliverable); the four hardening/perf fixes have no single matching WBS line and are logged here for visibility. | Maxene |

---

## How to add an entry

Add one row per meaningful unit of work (a PR, a working-session outcome, a decision), newest at the bottom. Reference the PR number where one exists so the entry can be traced back to the actual diff.
